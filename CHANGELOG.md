# 更新日志

本文件**面向使用者**：只说「你能多做什么、什么变好了、什么要注意」，
不写内部实现细节。

版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)：
`主版本.次版本.修订号`。预览期（`0.x`）可能会有破坏性改动，会在下面标出来。

---

> **排序约定**：**新的在最上面**（降序）。历史上一度是升序、末尾又挂了一段降序、
> 还有同一版本分成两条的——已整平。加新版本请加在**本行下面第一个**位置。

---

## [0.1.73]
### 修复
- **"权限明明开好了，App 还在提醒去开权限"** —— 这里有两个真问题，都修了：
  1. **状态只会记坏、不会记好**：屏幕采集失败过一次之后，执行端**一直**认为"画面出不来"，
     哪怕后面早就正常了。现在**新连接重新给一次机会**，而且**这一帧成功就把之前的毛病一笔勾销**
     （界面上的提醒会自己消失，不用重开 App）。
  2. **把"偶尔卡一下"说成了"你没给权限"**：macOS 的采集流**会间歇性地一帧都不来**（哪怕权限是好的）。
     以前这种情况也让你去开权限 —— 你跑去系统设置，看到的当然是"已经开好了"。
     现在执行端**先问系统**：权限在，就说"**权限是好的**，这一下多半只是采集流卡住了，正在重试"，
     **不再**给你那个用不上的按钮；只有权限真的缺，才给「去开权限」。
- **画面偶尔卡住时不再直接断开**：以前连续 3 次拿不到帧就掐掉整条流（表现就是"画面出不来"）。
  现在"这一下没来"和"真的坏了"**分开算**：前者会继续等（约半分钟到两分钟），一有画面就恢复。
- **给完权限要重启执行端**：这条现在写在提示里了 —— macOS 的规矩是"已经跑着的进程拿不到新给的权限"，
  所以你在系统设置里打开之后，**执行端要退出再启动一次**才生效。
## [0.1.72]
### 新功能
- **画面出不来时，手机上会直接告诉你为什么**：以前只有"连上了但一片黑"，现在会写清原因
  （比如"等画面超时 —— 最常见的原因是没有「屏幕录制」权限"），并给一个
  **「去开权限（屏幕录制）」**按钮，点一下**那台电脑上就会打开对应的设置页**。
  只在"那一页真能解决问题"时才给按钮 —— 按了没用的按钮比没有按钮更糟。
### 修复
- **「省流量」模式以前是坏的**：切过去之后**画面会整个黑掉**（参数确实换成了 640×416，
  但采集那边**实际送出来的还是屏幕原尺寸**，两边对不上就被判成"帧大小不符"而断开）。
  现在会在交付前把尺寸对齐。顺带把流量**真的降下来了**：同一段 20 帧，
  以前 151~216 KB，现在 **71 KB**（约 2~3 倍）。
- **采集失败的原因不再是天书**：以前只有一句 `采集失败 rc=5`；现在写成
  "**等画面超时**（5 秒内一帧都没来）… 最常见的原因是没有「屏幕录制」权限（系统设置 → 隐私与安全性 → 屏幕录制）"。
- **第一次在 Windows 上跑时，启动自检会提醒你先点防火墙的「允许访问」**，并说明后果：
  不光连不上，那个弹窗还会**占着前台**，手机上的点击会落到它身上（看起来像"操作没反应"）。
## [0.1.71]

### 新功能

- **Windows 上也能把手机当键鼠用了**：点击、拖拽、滚轮、具名键（ESC/TAB/方向键/回车…）都对；
  中文和 emoji 走 `KEYEVENTF_UNICODE`，**不经过键盘布局**，所以在任何输入法状态下都打得出来。
  真机验过：手机上点一下，那台 Windows 机器上收到的归一化坐标到屏幕点是**逐位对得上**的。
- **注入没成功时会明说**：目标窗口权限更高（UIPI）、或者执行端不在你的桌面会话里时，
  日志会写清楚「⚠️ /screen 点击没有真的注入：…」，而不是让你对着一动不动的屏幕猜。

### 注意

- **Windows 上要控制你的桌面，执行端必须跑在你自己的登录会话里**。挂在 SSH 会话或系统服务里时，
  它**看不到你的画面、点了也不会动**（自检会直接告诉你「采到的是整屏同色」）—— 开机自启是最省事的做法。
- **Windows 上，如果电脑弹出了别的窗口（尤其是安全/权限类的对话框），操作会落到它身上** ——
  这类对话框的权限级别更高，系统会**直接丢掉**我们的输入（日志里看不出异常）。
- **Windows 上，手机的操作是发给「电脑上当前最前面那个窗口」的**。所以用之前**先点一下你要操作的那个窗口**；
  如果目标窗口没在前台，表现是"手机上操作了、电脑上没反应"（日志里一切正常 —— 因为确实发出去了）。
  这也意味着**别的程序弹窗抢了前台时，操作会落到那个弹窗上** —— 这是 Windows 输入注入的固有语义。

## [0.1.70]

### 新功能

- **结构化会话里可以传图了**：在转录视图点「图片」（**只有 agent 自己说支持图片时才会出现这个按钮**），
  从相册选一张，它会缩好、编好，随这一轮一起发过去。链路是端到端验过的 —— 真机上点下去，
  执行端日志出现「ACP 发一轮：（附 1 张图）」，对面的 opencode 也确实收到了并对着图回话。
- **agent 起不来时，现在会说清它自己的原因**：以前打开一个结构化 CLI 失败，只有一句"启动失败"；
  现在把 CLI 在 stderr 上最后说的话一并带出来（例如"要登录"），照着做就能自救。

### 注意

- **macOS 的安装包暂时只有 Apple 芯片（Apple Silicon）那一份**：Intel 的那份要在一台 Intel 机器上
  原生打包，而我们目前用的云端打包因为**账户计费**没跑起来。**已经装了 Intel 版的用户继续能用**，
  只是暂时收不到新版本；等计费恢复就会补上。
- 产物的命名跟着改了：`macos11+-silicon-<版本>.zip` / `AirControl-macos11+-silicon-<版本>.dmg`
  （`11+` 是"大于等于 macOS 11"；`silicon` / `intel` 指芯片）。

## [0.1.69]

**修一个"用户看到会以为包坏了"的问题**：上一版的 macOS 安装包，系统可能报「**已损坏**」——
那不是包坏了，是**签名封条失效**。这版修好了。

### 修好

- **macOS 安装包的签名封条失效**：打包时是**先签名、后往包里放自带的 tmux**，签名里的封条
  当场就破了。后果是系统可能把它判成「已损坏」—— 而这句话**对用户没有任何可操作性**
  （对比之下「未识别的开发者」至少还能右键打开）。现在放完 tmux **会重新签名并当场验封条**，
  验不过就让打包**直接失败**（一个会显示"已损坏"的包不该被发出去）。

### 注意

- **macOS 首次打开会被系统拦一下**（从网上下载的 `.app` 都这样，不是包坏了）：在"访达"里
  **按住 Control 点**图标 → 打开 → 再点一次"打开"；或者命令行：
  `xattr -d com.apple.quarantine /Applications/AirControl.app`。
  详见 `docs/distribution.md` §2.6。
- 想彻底不再出现这一拦，需要 Apple 开发者账号做签名与公证（每年 99 美元）—— 目前没有，
  所以走上面那两条。

## [0.1.68]

**电脑上终于有个"管理端"了** —— 在这之前，执行端在桌面上是**完全隐形**的：没有窗口、没有图标，
出了事只能去翻终端或者看日志。

### 新增

- **桌面管理端**：双击图标就有窗口了（macOS 的 `.app` 里就是它）。窗口里能看到：服务在不在跑
  （进程号、端口、版本、运行了多久）、**连接码和可用地址**、屏幕控制与文字输入**有没有拿到权限**
  （缺哪个、该去哪一页开，都写清楚）、以及**日志尾巴**（出了问题不用再去翻文件）。
  还有「启动服务 / 停止服务 / 重启执行端 / 打开实例目录」几个按钮。
- **启动时会先看服务在没在跑**：已经在跑就**只开窗口**接上去（**绝不会偷偷起第二个** ——
  两个执行端抢同一份状态目录会把配对和会话弄乱）；没在跑才起一个。
- **关闭时会问一句**：「一起退出服务进程」还是「只关这个窗口」（可以记住，下次不再问）。
  关掉窗口**不会**顺手把服务停掉 —— 那是两件事。
- **`daemon status`**：命令行里一句话看服务在没在跑（在跑返回 0、没在跑返回 1），
  脚本里也能直接用。

### 修好

- **Windows 上远程终端里的中文不再乱码**：以前终端跑在系统默认代码页（中文机器是 936）下，
  中文在别的工具里看就是乱码；现在启动时把它顶成 UTF-8。

### 注意
- **macOS 首次打开会被系统拦一下**（下载来的 `.app` 都这样，不是包坏了）：在"访达"里**按住 Control 点**
  图标 → 打开 → 再点一次"打开"即可；或者命令行 `xattr -d com.apple.quarantine /Applications/AirControl.app`。
  ⚠️ **这一版的 dmg 会报"已损坏"而不是"未识别的开发者"** —— 那是打包顺序的问题（先签名、后往包里
  放 tmux，签名的封条就失效了），**下个版本修好**；上面两条自救办法**照样有效**。

- **macOS 的权限是"按哪个程序"授权的** —— 换了新版本的 `.app`，**要重新授权一次**
  （屏幕录制、辅助功能）。管理窗口里会实时显示缺哪个、并给一个「去开权限」的按钮。
- 授权之后**要重启执行端才生效**：现在这件事由服务自己盯着 —— 权限一给齐就自动重启一次
  （不用你手动重启，也不用非得开着窗口）。

## [0.1.67]

**上一版的"下载断了能接着下"其实没生效** —— 这版把它修好了。这两个问题都只有真机上才看得见。

### 修好

- **接着下从来没生效过**：上一版宣传的断点续传，实际上每次都是从头下 —— 原因是代码里
  先"把半截读回来"，紧接着又有一句旧的"清空"把它清掉了（我加新逻辑时忘了删旧的）。
  现在**真的会接着下**了：真机实测，手工放一个 10 块的半截 → 点下载 → 那台机器上打出
  `下载续传：resume-e2e.bin 从第 10 块开始`，最终文件的 md5 与源文件完全相同。
- **半截文件和它的"进度记录"可能对不上**：磁盘写失败时（权限、空间、被中断），进度记录
  却照样往前记一格 —— 这会让下一次续传**从错的位置接上**，拼出一个坏文件而且**不报错**。
  现在**写不进去就不记账**，并在执行端日志里说一声。

### 改进

- **执行端的日志带时间戳了**（`12:03:41 [daemon] 下载：…`）。没有它的时候，`tail` 出来的
  可能是几分钟前那一次，很容易把人引到错误的方向（我自己就被骗过一轮）。
  配对那张卡片**没有**加，免得把框画歪。

## [0.1.66]

**下载断了能接着下了** —— 传大文件最烦的那件事。

### 新增

- **断点续传**：下到一半断了（网络抖了、切出去一会儿、那台机器的执行端重启了），
  现在**再点一次「下载」就接着下**，不用从头再来。手机上没有多出什么按钮 ——
  反正你本来就会再点一次。
- **App 被系统杀掉之后也能接着下**：下到一半的数据会**存在手机上**（App 自己的目录里，
  不会出现在你的文件夹里添乱），下次进来点「下载」就从断的地方继续。
  断了的时候提示会写清楚已经拿到多少，比如"下载断了（已拿到 42 块 / 10 MB）——
  再点一次「下载」就接着下"。

### 说明

- 续传只在**同一个文件**上生效；换了文件就是从零开始（这是故意的）。
- 一次选多个文件上传（上一版加的）在**模拟器**上没能验到多选（那台模拟器的文件界面
  只肯返回一个文件）；真机上应当是正常的，但这话是推断，不是验证。

## [0.1.65]

**修掉一个"传文件传不上去、还看不出来"的问题** —— 这版没什么新花样，就是把上一版
里那个坑填了。

### 修好

- **上传会静默失败**：上一版里，如果你在系统选文件的界面上**挑得慢一点**（比如翻目录
  找个文件，三十秒以上），回到 App 之后**什么都不会发生** —— 提示一直停在"选一个文件…"，
  而那台机器上一条记录都没有，看起来就像按钮坏了。原因是 App 等结果的**超时太短
  （30 秒）**，挑得慢就错过了。现在放宽到 **5 分钟**，正常挑文件不会再撞上。
  （"选图片"那条路用的是同一个超时，也一起修好了。）

### 说明

- **一次选多个文件**已经做了（选完一个一个传，不并发，免得把心跳和画面挤住），
  但在**模拟器**上它的文件界面只肯返回一个文件，所以"多选"这条还没在真机上验过 ——
  真机上应当是正常的，但**这话是推断，不是验证**，先如实写在这儿。

## [0.1.64]

**远程文件管理齐了**：以前只能"看一个文件、下载一个文件、传上去"，
现在**先列目录再动手** —— 手机上能像文件管理器一样看那台机器上有什么。

### 新增

- **列目录**：文件面板多了「列一下」。列出来的东西**目录在前、按名字排**，
  点目录**进去**、点文件**直接读**，还有「← 上一层」。一行概括写着
  「15 个目录 · 8 个文件」；**要是一屏放不下，会明说"还有更多"**——
  不这么说的话，人会以为目录就这么多。
- **上传可以指定传到哪个目录**了：在上面的路径框里填 `build/`（或者某个文件，
  表示"传到它旁边"），传上去的文件就落在那里；留空还是老样子（工作目录根下）。

### 修好

- **macOS 上双击安装后不再是"点了没反应"**：以前它是个完全隐形的程序
  （没有窗口、没有 Dock 图标）——第一次弹授权、授权完什么都不发生、再点还是没界面，
  很多人会以为装坏了。现在**会弹一个状态窗口**：连接码、可用地址、**实时的权限状态**
  （缺哪个、为什么缺）、「去开权限」，以及**权限给齐后自动重启**
  （屏幕录制/辅助功能这类权限，授了必须重启才生效——以前它既不重启也不说）。

### 注意

- 列目录一次最多给 300 项（超了会标明"还有更多"）；只允许看**执行端工作目录里面**。

## [0.1.63]

**手机上能把文件传到电脑了** —— 以前只能"从电脑下载到手机"（那个方向早就通了），
现在**两个方向都通**：手机里的一张照片、一个日志，选一下就能放到那台机器上。

### 新增

- **「传上去」按钮**（在文件面板里，紧挨着「下载」）：选手机上的任意文件 → 分块传过去 →
  传完会告诉你**它到底落在哪**（完整路径 + 字节数）。
- **传坏了不会留半个文件**：传输中途断了、或者字节数对不上，目标机器上**不会**
  出现一个看起来完整、其实是坏的文件 —— 宁可报"没传成"。
- 手机端限制单文件 **64MB**（再大请用别的方式）；执行端另有 **256MB** 上限。

### 修好

- **`@` 找文件不再只对 Codex 有效**：以前用别的 AI（Claude / Gemini…）时，打 `@` 会回一句
  "文件搜索要 codex 会话"。现在执行端自己有搜索，**任何会话都能用**。
- **画面卡的时候会告诉你为什么**：屏幕面板那行字现在会写"画面 3fps · 省流量画质
  （网慢，执行端自动降的）" —— 让"变卡"这件事有解释，而不是让人以为 App 坏了。
- **版本检查更快**：本机查更新不再每次都去够外网（上游不通时原来是每次等六七秒）。

### 注意

- 上传的**目标路径就在执行端的工作目录里**（用原文件名），不能指定到任意绝对路径 ——
  这是有意的：路径是手机给的，不能让它写到不该写的地方。

## 计划中（未发布）

> 这是**还没做**的。做完的会进下面的版本节。

- Windows / Linux 上更好的屏幕捕获

## [0.1.62] — 2026-09-23

### 网慢的时候，画面不再"越拖越旧"

屏幕流现在会**自己看情况调帧率**：发一帧要花的時間变长（手机网络差、对面忙不过来），
它就主动降一点帧率；缓过来了再升回去。

- **不会超**你在省流量模式里选的那个上限（选了 1fps 就一直是最多 1fps）；
- **降得快、升得慢**：卡住了要马上缓解，缓过来则多观察几拍再升 —— 免得刚顺一下又卡住；
- **有下限**：再慢也保证每秒至少一帧，不会把画面"冻住"；
- **抖一下不改**：偶尔一帧慢不会动它，避免帧率来回跳（那比固定还难受）。

### 执行端会告诉你"为什么卡"

统计行里现在带上**发一帧花了多久**：

```
[daemon] /screen 1280x832 @5fps 画质0.72：20 帧 / 4.0 秒 = 34.4 KB，即 8.5 KB/秒；发包约 1.2ms 5fps
```

这一个数就能回答"为什么现在只有 3fps"——是链路慢（发包耗时接近预算），还是别的原因。

### 说明

- 这次的判据是**真的量出来的**：用测试钩子人为把发送拖慢，执行端一路 `5→4→3→2fps`
  降下去；钩子失效后又 `2→3→4→5fps` 升回上限（帧率统计从 20 帧/13 秒回到 20 帧/4 秒）。

## [0.1.61] — 2026-09-23

### 省流量真的省了一半（这次是量出来的）

上一版的「省流量」只降了**分辨率**和**帧率**，这一版把**画质**也一起收
（三个杠杆一起用才是量级上的差别）：

| 档位 | 尺寸 / 帧率 / 画质 | 实测 |
|---|---|---|
| 正常 | 1280 宽 · 5fps · 画质 0.72 | **8.1 KB/秒** |
| 省流量 | 640 宽 · 1fps · 画质 0.40 | **4.0 KB/秒** |

（数字来自执行端自己报的统计，手机上切一次档就能看到差别。）

### 你能看见流量花在哪了

执行端现在会定期报**每秒字节数**（不是"每帧多少" —— 那个数在"只在变化时才发"
的屏幕流里**会骗人** ✗：1fps 的一帧装着一整秒的变化，5fps 的一帧只装 0.2 秒的，
两者根本没法比）。手机上的流量数字照旧走心跳回来。

### 说明

- 画质是**档位**（低 / 正常 / 高）而不是随便填的数字 —— 免得把对面执行端拖死。
- 不带档位参数的老客户端行为**完全不变**（按正常档）。

## [0.1.60] — 2026-09-23

### 手机上能按的组合键，多了

屏幕面板键条上多了 **SHIFT / CTRL / ALT** 三个按钮。它们不是装饰 ——
按下去之后，下一个键会**带着修饰键**发到对面：

- **Shift+Tab**：很多命令行界面里是"往回切"（终端里到的是 `ESC[Z` ✓）
- **Ctrl+C / Ctrl+D / Ctrl+Z**：中断、结束输入、挂起 —— 这些是命令行里最常用的三个
- **Cmd+W** 之类的窗口快捷键

以前这些**根本发不出去**：按键表里只有方向键、回车、Tab 这些"具名键"，
字母和数字压根没有 ✗ —— 你按了 Ctrl+C，对面什么都没发生，而手机上看不出哪里不对。

### 说明

- 修饰键是**按下状态**（高亮显示），按一次开、再按一次关；发完一个键**不会自动弹起**，
  方便连着按几次（比如连按几个 Shift+Tab ✓）。
- 屏幕这条键条的修饰键和终端那条**各管各的** —— 两条通道去向不同（一个发到对面
  当前焦点、一个写进终端的 PTY），共用一个开关会让人搞不清自己到底给谁加了 Shift。

## [0.1.59] — 2026-09-23

### 手机上能把那台机器的文件**改**了（改错了也找得回来）

上一版能"看"和"下载"，这一版补上第三件：**改**。

- 看完文件点**「改一下」**，内容就变成可编辑的（**已经帮你填好原文**），改完点两次
  「写回」→「确认写回」才真的写 —— 写盘没有撤销键，所以刻意让它需要确认。
- **执行端一定会先备份**：原文留在同目录的 `<文件名>.aircontrol-bak`。
  改错了、或者对面 AI 又改了，随时能自己找回来。
- 规矩是执行端定的，不靠界面自觉：**只改已经存在的文本文件**（不会顺手新建）、
  **二进制直接拒绝**（把压缩包当文本写回去＝毁掉它）、有大小上限、
  **原子替换**（中途失败也不会留下"一半新一半旧"的文件）。

### 修：升级之后不用再重新配对

以前升级一次（或者对面执行端重启一次），手机上的连接记录会显示
**「连接码已失效，请修改后连接」**，看起来像是得重新配对一遍 —— 其实**根本不用**：
设备令牌一直好好的，只是 App 没把它读回来。现在会正常显示「重新连接」，点一下就连上。

### 说明

- 这三个动作（看 / 下载 / 改）都走**执行端**那台机器的权限，跟你在那台机器上自己动手一样。
- 读不到、写不成时说的是**具体原因**（找不到 / 是目录 / 是二进制 / 太大），不会静默失败。

## [0.1.58] — 2026-09-23

### 手机上能看那台机器的文件了

以前要看对面一个文件，只能在终端里 `cat` —— 手机上是一屏刷过去的字节流。
现在「控制」里多了**看文件**：

- **看**：写个路径（相对路径按执行端的工作目录算），内容直接显示出来。
  文本文件才给看 —— **二进制会明确回绝**，而不是塞一屏乱码给你。
  大文件**会明说"只显示了前 64KB"**，不让你把半截日志当成全文。
- **下载**：任意文件都能**存进手机的「下载」**里 —— 图片、压缩包、日志全文都行，
  存完在系统「文件」里就能找到、别的 App 也打得开（不需要任何权限）。
  手机上会写清**存到哪儿了**。

### 说明

- 读不到时说的就是"读不到"（找不到 / 是目录 / 是二进制），**不会给你一个空文件**
  —— 那会让人以为"这文件是空的"，然后去找一个不存在的问题。
- 下载是**先检查再发**：太大了、是目录、不存在，都会在一开始就说清，
  不会传一半停下留个半截文件给你。

## [0.1.57] — 2026-09-23

### Codex 现在真的能用了

以前手机指挥 Codex 只能看终端里刷屏 —— 它问"这条命令能跑吗"，你得在终端里按 y。
这一版把它接成了**结构化**的：

- **手机上点一下批准**：Codex 要跑命令时，手机上出现一张卡，**带着完整命令原文和目录**
  —— 看清楚再决定。允许 / 这个会话都允许 / 拒绝 / 拒绝并打断，四个选项都在。
- **看得到它在干什么**：它说的话、跑的命令、命令的输出，以列表推过来，
  不再是一屏字节流；**上下文占用**（用了多少 / 窗口多大）也跟着显示。
  *（Codex 不报金额，所以成本那条不显示 —— 拿不到就不显示，不显示 0。）*
- **`@` 找文件**：输入框里打 `@` 加几个字，用 Codex 自己的文件索引搜，点一下插进输入框。
  手机上打全路径几乎不可能，这个是真省事。

### 修掉两个"明明做了、却用不了"

- **Codex 在手机上点不到**：智能体那一行放不下之后**直接裁掉**（既不换行也不能滚），
  Codex 正好在被裁掉的那半边。现在分行摆，每个都点得到。
- **候选列表点不中**：`@` 的候选原来是横着排的小芯片，在窄布局里被挤到屏幕边上裁掉。
  改成竖排、每行整行宽 —— 手指不用瞄准。

## [0.1.56] — 2026-09-22

### 屏幕视图：现在**能滚、能拖、按键也能送过去**

上一版让屏幕"点得动、打得出字"，这一版把它补成能用的样子：

- **拖动 = 滚动**（跟手机上刷内容一样）。长文档、长网页不用再一格一格挪。
- **长按之后不松手再拖 = 拖拽**：拖窗口、拖滑杆、拖选区都行。
- **一排按键**（ESC / TAB / ← ↑ ↓ → / 回车）就在画面下面，
  方向键选列表、回车确认、ESC 取消 —— 屏幕上那些"不是字符、是命令"的键终于有了。
- **执行端不再抢前台**：以前它一启动就会变成"最前面的那个程序"，
  于是你在自己电脑上打字会**打不进去**（字都进了它那儿，而它什么都不做）。
  现在它安安静静待在后面。

### 说明

- 滚动 / 拖拽 / 按键都**和点击同一个权限**（macOS 的「辅助功能」）——
  没授权时不会装作能用：屏幕上会直接写清缺什么，并给「去开权限」。
- 按钮标注的键名（`ESC` / `UP` …）与终端那条键条是同一套；
  两边的键条**各管各的通道**，不会串。

## [0.1.55] — 2026-09-22

### 屏幕视图：现在**点得动、也打得出字**

以前屏幕视图只能看 —— 点画面没反应，也没法往对面的输入框里打字。
现在：**点一下画面 = 在那台机器上点一下**；屏幕下面还多了一行输入框，
字会打进对面**当前光标所在的地方**（中文、表情都行，跟键盘布局无关）。

- **要一次授权**：macOS 的「辅助功能」权限，和「屏幕录制」**不是同一个**。
  没授权时不会装作能用 —— 屏幕上直接写「只能看，点不动 —— 缺什么」，
  并给一个「去开权限」按钮，按下去**在那台电脑上**打开对应设置页。
- **怎么用**：先点画面、把光标点进对方的输入框，再在下面那行打字、按「打进去」。
  点在画面之外的黑边上**不算点击**（不会误触）。
- **限制说清楚**：目前只有「点一下」和「打一串字」两种操作，拖动 / 滚轮 / 方向键还没有；
  Windows / Linux 的执行端还不支持注入，界面会**如实说明**而不是点了没反应。

### 修了两个「看起来成功了、其实没生效」的问题

- 「去开权限」按钮以前**日志说打开了、而那台电脑上什么都没发生**（设置页根本没起来）。
- macOS 上的屏幕点击：坐标算得对、消息也送到了，**就是点不动** ——
  根因是坐标结构体传参方式错了（事件"创建成功"、坐标却是偏的）。现在修好了。
- 另外修掉一处：连上后去要通知权限时，万一出岔子会**打断界面状态刷新**
  （表现为"已连接但终端一片空白"）。

## [0.1.54] — 2026-09-22

### 修了一个「发不出消息」的问题

同时开着**终端**和**转录**两个视图时（左右并排），转录的输入框会被终端的**特殊按键条**
盖住 —— 点「发送」完全没有反应，也没有任何报错。现在这种情况下的处理是：
**转录开着时就把键条收起来**，输入框因此完整可见、能正常发送。

- **代价说清楚**：两个视图同开时，终端**没有** ESC/TAB/方向键那些特殊键。
  只看终端时键条照旧。我们选了"能发消息"这一边。
- 真正的布局修法还没做（面板内部的高度分配），代码里留了标记。

### 安装方式：macOS 也有双击安装包了

上一版只在 CI 上产安装器，本地发布的版本里没有。现在**两条路一样**：
macOS 会多出一个 `.dmg`（自带运行时，**不用先装 Java**），与 zip、APK 一起发布。

---
## [0.1.53] — 2026-09-22

这一版把「连接安全」补完了，并让 **Claude 也能用转录视图**。

### 传输加密：跨网络不再只能靠明文

以前连接是**明文**的，跨网络实际靠 Tailscale 之类的隧道兜着。现在执行端可以开一个
**加密端口**（`--wss-port`），连接串里会带上**证书指纹**，手机端照着它
**只认那一张证书** —— 换了证书就拒连。

- 表单里新增「**证书指纹（可选）**」一栏，手输地址也能用加密连接了。
  留空仍是明文（内网/本机够用）。
- 指纹不匹配时，界面会**说清楚**：「对方的证书和上次不一样（可能是它换过证书，
  也可能有人在中间）」—— 而不是含糊地报「连不上」。

### Claude 也能用转录视图了

Claude 没有结构化会话，所以以前只能走终端。现在它走**官方的一次性问答接口**
（`--output-format stream-json`）—— 于是转录视图对它也能用：
**可以问、可以看回答、可以看这一轮花了多少钱**。

- 这条路是**只读**的：没有工具审批。界面上会明说，不让你以为按钮坏了。
- 用的是官方接口，**没有依赖任何非官方约定**。

### 流量看得见、也能省

- 「心跳」按钮旁边会显示本次连接**收了多少、发了多少**，以及其中**屏幕占了多少**
  —— 「流量怎么这么大」这个问题终于有答案了（通常是屏幕流）。
- 新增「**省流量**」开关（视图行里，只在看屏幕时出现，**会记住**）：
  屏幕流降到 640 宽 1fps，实测**约 2.9 倍**的差距。

### 其他

- **端口被占时给人话**：以前是一大段 Java 异常栈，现在直接告诉你
  「端口被占了」+ 换个端口或看看谁占着。
- **修了一个悄悄改用你配置的 bug**：点「新建连接」时会带上**上一条机器的证书指纹**，
  于是连新机器必然失败 —— 现在会清空。
- 界面文案里几处会**原样显示**的 `**` 已改掉。

### 安装方式多了「安装器」

除了原来的 zip（需要自己装 Java），现在还有**自带运行时的安装器**：
macOS `.dmg`、Linux `.deb`、Windows `.msi` —— **双击就能装，不用先装 Java**。
体积大一些（约 55-72 MB），按需选择。

---
## [0.1.52] — 2026-09-22

这一版让**同一台机器上可以同时跑两个执行端**，并给了一个能管永久配对码的命令。

### 连接历史：能不能连，一眼看得出来

以前只有「连上了 → 重新连接 / 否则 → 连接」。于是**用已经用过的一次性连接码去点，
按钮上看不出任何异常，点了只会失败** —— 你得自己想起来"哦那是一次性的"。

现在判据是（按强弱排）：

- **配对过（有设备令牌）→ 「重新连接」**。令牌是配对成功后执行端发的长随机串，
  带着它重连**根本不需要连接码**。
- **存的是永久码（6 位）→ 「重新连接」**。
- **存的是 4 位一次性码 → 按钮变成「填新码」**，状态行写明
  **「连接码已失效，请修改后连接」**。点「填新码」直接进修改。

### 执行端：同机跑两个实例（开发版与发布版并存）

新增 `--home <目录>` 指定**实例状态目录**（默认 `~/.aircontrol`，行为不变）。

以后**发布版**跑 `8080`、**开发版**跑 `8081`，各自有自己的设备令牌与永久码，
**互不覆盖** —— 也就是说：开发时随便折腾，不会把你手机上那个连着的发布版弄掉。

> 升级只换程序。状态目录不动，所以**永久码与已配对的设备都还在原地**，
> 不需要重新配对。

### 执行端：`daemon perm` 管永久配对码

以前只能进控制台按 `d` 换，随机生成、当场显示明文，之后只剩哈希。
现在可以：

```
daemon perm --set <6位码>   # 用一个指定的码
daemon perm --rotate        # 随机生成一个并打印（唯一能看到明文的时刻）
daemon perm --check <码>    # 这个码还对吗
daemon perm --clear         # 删掉（已配对设备不受影响）
```

> 注意：改完要**重启执行端**才生效 —— 跑着的进程内存里还是旧的那个。

---
## [0.1.51] — 2026-09-22

这一版改动不多，但有一件挺要紧：**通知被关掉时，现在会明说。**

### 通知被关了，你会看到

之前有个不声不响的坑：如果系统的通知权限被关掉（或者你在弹窗上点了「不允许」），
通知会**发出去、被系统丢掉** —— 而界面上**看不出任何差别**。
后果是：AI 卡住等你批准的时候，**叫不醒你**，而你以为一切正常。

现在只要连着的时候通知是关的，顶上就会有一块提示：

```
通知被关了 —— 智能体卡住时叫不醒你
[ 去开启通知 ]  [ 知道了 ]
```

点「去开启通知」会直接跳到系统的通知设置页。你自己去设置里开好、回到 App，
提示会**自己消失**（不用重启）。

> 为什么要专门做这个：这个 App 的价值是"你只在关键时刻出手"。
> 而"关键时刻能不能叫醒你"如果坏了却看不出来，那前半句就是空的。

### 内部（用户不用管，但影响可靠性）

- 执行端**关闭时更干净**：以前关闭时后台任务可能还在跑，而它依赖的通道已经在拆了，
  于是会抛出没人接的异常。现在会**等它收尾**（有超时兜底，不会被卡住的收尾拖死）。
- 补了一批**自动化检查**（协议副本一致性、转录合并规则、界面线程纪律、
  文档完整性），并接进了 CI —— 每次推送都会跑。
- 发布流程：**少了凭据会在几秒内报错**，不再先白构建七分钟。

---
## [0.1.50] — 2026-09-22

这一版修掉了一个**会让应用直接崩掉**的 bug，另外两件事是"少问一句、多看一步"。

### 修：扫码后取消会把应用弄崩

点「打开相机扫码」、打开相机后又按返回 —— **应用会直接崩掉**。

原因是相机界面是全屏的，退回来那一刻，界面还没重新显示出来，这时去碰界面框架的内部
接口就会失败。同类问题之前也修过一次（选图片那条路），这次把扫码那条一起改成同一种做法：
**先把结果放好，再由界面自己在下一帧去取**。

受影响的是「扫码连接」这条路。同一次改动也把配对流程的通路验通了。

### 连过的机器不再重复问

以前每次扫码或点连接链接，都要再点一次「连接」。现在：**那台机器你以前连过**
（在「连接历史」里），就直接连上，不再拦一道。

**换一台机器、或者换了端口，照样会问。** 想反悔就把「连接历史」里那条删掉 —— 删了就会重新问。

> 为什么这么定：同一台机器每次都要问一遍的话，久了就变成无脑点「连接」，
> 那道确认反而等于没有。让它只出现在**真正新的目标**上，人才会认真看。

### 计划看得见了

AI 说它打算分几步做、现在做到第几步 —— 现在在转录视图里以**清单**显示：

```
计划 1/3
● 读一下 README        （已完成，压暗）
◐ 改代码并补测试        （正在做）
○ 跑全量测试并整理文档   （还没开始）
```

三种状态配三种符号和颜色，**形状和颜色都给**（色弱用户看形状也能分清）。

> 说明：这需要 AI 主动报计划。实测你用的 OpenCode 目前**不报**，所以平时看不到；
> 换一个会报计划的 AI 就会出现。

---
## [0.1.49] — 2026-09-22

接着上一版：**手机上可以直接给 AI 发图片了**，另外修了两个"看着不对劲但说不出哪不对"的问题。

### 可以给 AI 发图片

在转录视图里，输入框旁边多了「图片」按钮 —— 点一下从相册选一张，发出去。

- **图片会在手机上先缩小**（长边 1600）。相册原图动辄好几兆，不缩的话
  一条消息就可能超出执行端的接收上限，表现是"发出去没反应"。
- **这个按钮只在 AI 自己说支持图片时才出现**，免得你点了没反应。
- ⚠️ **一个要提前说清的事**：AI 说支持图片，指的是**它愿意收**，
  **不等于它背后的模型看得懂**。实测就遇到过：CLI 说支持，但你发图过去，
  它回的是「我读不了图，这个模型不支持图片输入」。
  那是**对端**的事，我们照实把它的原话显示出来 —— 你看到的永远是真相。

### 批准/拒绝之后，卡片会收掉了

以前点完批准或拒绝，那张卡片**会一直挂在屏幕上**，状态点也一直显示
「待你处理」—— 而实际上早就不需要你处理了。现在点完就收掉。

> 顺带说明：这个修复之前，**再点一次是完全没有反应的**（静默无操作）。
> 一个假的"待你处理"，比没有提示更糟。

### 审批的两个方向都在真机上验过了

用 OpenCode 真的触发了一次权限询问，**批准**和**拒绝**都在手机上点过：
批准后命令真的执行了，拒绝后命令**没有**执行 —— 而且两种情况那一轮都正常收尾。

---
## [0.1.48] — 2026-09-21
结构化会话：手机可以直接和 AI 对话了。

### 像终端一样看着 AI 回答

如果你用的 AI CLI 支持 **ACP**（一个行业标准协议，不是某家私有格式），
打开它会进一个新视图「转录」：回答**一块一块地滚出来**，并且**自动跟到最新**——
不用自己往下扒拉。

终端视图还在，两者是**两条路**：终端是"CLI 原样的界面 + 按键"，
转录是"事件流 + 可点的按钮"。同一个 CLI 只能走其中一条。
支持哪种会标在芯片上（带 ✦ 的走转录）。

### 批准可以直接点，不用去终端里按 y

以前审批只能把请求递到你面前、再往终端注入按键。现在**弹一张可点的卡片**，
完整命令不截断。

⚠️ 有一种情况按钮会变：如果那个 CLI **只提供"以后都允许 / 以后都拒绝"**
（没有"仅此一次"），按钮会写成 **「永久允许 / 永久拒绝」**，标题标上「（永久授权）」——
免得你以为批的只是这一次。

### 命令面板

CLI 自己声明它支持哪些斜杠命令（实测 OpenCode 报了 **89 个**）。
现在可以筛，点一下就把命令填进输入框。

### 成本与上下文可见

结构化会话里能看到**上下文占用**与**费用**。拿不到就**不显示**——
不显示 0，那是撒谎。

### 电脑屏幕上能点、能打中文了

「屏幕」视图以前只能看。现在能点、能输入，
中文通过 Unicode 直接送，**不受执行端键盘布局影响**。

### 说清「哪些 CLI 真的能用这条路」

不是"文档里好像支持"就算——每一个都实测握过手才列进去：

| CLI | 结果 |
|---|---|
| Gemini CLI / Qwen Code | ✅ |
| OpenCode | ✅（完整对话实测跑通） |
| Cline | ✅ |
| Claude Code | 走另一条路（hooks） |
| Codex | 走另一条路（app-server） |

### ⚠️ 这个版本要注意的

- **先升 App 再升执行端**（或同时）。协议版本升到 **2**：协议 1 的旧 App
  连上新执行端会被**明确拒绝**并提示升级——这是有意的，
  总好过"列表空了、点了没反应"。
- 屏幕视图仍是**最小远程桌面**：只做点击与文字输入，**不做拖拽 / 多指手势 / 窗口管理**。

## [0.1.47] — 2026-09-21
Phase A：安装与工具管理。

### 执行端一键安装会自己备好 Java

不用你先装 Java 17。`install.sh` 自动准备便携 JRE，**走国内镜像**（实测比官方快 40 倍）。

### AI CLI 的配置与别名

- 按每个 CLI **自己的原生格式**生成配置文件，放在 `~/.aircontrol/ai-auth/` 下，
  **不碰它的官方默认目录**。
- 别名是五段式：`airctl-<工具>-<提供商>-<模型>-<权限>`，写进你的 shell 配置，
  只动我们自己的那个标记块（有备份、幂等、原子写）。
- 配置模板是「注释即文档」的 TOML——官方格式变了**不用等我们发版**。

### 手机上一键启动

抽屉里的设备行点「+」→ 列出服务端配好的别名 → 点一下就启动。
Claude Code 实测能起来，且 `bypass permissions on`。

### 服务端本地配置界面

`daemon setup`——终端交互式，**没有桌面也能用**。
写入权限**只在服务端本地**，手机端只读（那类操作权限太敏感，不该由远程设备触发）。

### Windows

启动方式按平台探测（PowerShell profile 与 pwsh 7 是两个不同文件）。
没有 tmux 时 `/term` 降级为登录 PowerShell，用户自己的 profile 照常加载——
所以别名机制在 Windows 上同样有效。

## [0.1.46] — 2026-09-21
交互重做 + 安全加固。

### 终端输入交还给终端

去掉了自建的输入框。**IME 直接连通 PTY**——这才是终端该有的样子：
输入法候选、组合、粘贴都由系统处理，不再经我们转一道。

### 命令输入区重做

多行、回车发送、键盘上推、历史翻阅。

### 修掉的真问题

- 截图视图「点了亮着但没内容」。
- 状态点的语义接上了：「等待你处理」不再**只靠颜色**区分。
- 浮层高度没扣键盘，设置页的输入框会被顶出去。

### 安全

多轮审计修掉若干认证/授权缺口；发布脚本不再静默失败。

## [0.1.45] — 2026-09-20
### 没 tmux 时终端空白，现在说清为什么

上一版修好了"没 tmux 就连不上"，但连上之后**终端是一块空白** ——
因为会话建不出来，没有东西可显示。这本身合理，
但用户看到空白只会以为"还没加载出来"然后一直等 ✗

现在执行端把 tmux 能力也报上来，手机端据此显示：

```
没有会话可显示
<主机名>
这台机器没装 tmux，会话管理用不了
连上、命令、审批都不受影响
想用会话：apt install tmux
```

⚠️ 措辞刻意**不用"终端用不了"** —— 连接是好的、命令也能发，
缺的只是**会话管理**。说成"终端坏了"会把人吓跑。

### 那个"随机挂"的 E2E 测试修好了

`DaemonE2ETest` 跑全量时随机挂一条，单独跑就过 —— 典型的测试隔离问题。

根因：收尾只给了 `stop(100, 500)`（**100ms 宽限 + 500ms 超时**），
那个 daemon 的推送/监听循环还在跑就被硬停，异常**泄漏到下一个测试**，
让下一个测试以 `UncaughtExceptionsBeforeTest` 失败。

现在给 2 秒宽限 / 5 秒超时，并等它停稳。

> **连跑三遍全量，全过。**
>
> 一个"随机挂"的测试比没有测试更糟 —— 它会训练人忽略红色 ✗

---

## [0.1.44] — 2026-09-20
### 🎯 没装 tmux 就连不上（真机实测抓到的，根因）

**症状**：手机连 Linux 服务器时**一直停在「重连中」**，而执行端日志显示
**设备已授权** —— 什么错都没有，就是连不上。

**根因**（两层叠在一起）：

```kotlin
private fun run(vararg cmd: String): ProcessResult {
    val proc = ProcessBuilder(*cmd).start()      // ← tmux 不存在时抛 IOException
    ...
    val exitCode = try { proc.waitFor() } catch (e: IOException) { -1 }   // ← 死代码
}
```

① **`ProcessBuilder.start()` 在可执行文件不存在时抛 `IOException`** ——
   而那个 try/catch 包在了 `waitFor()` 上，**它抛的是 `InterruptedException`**
   ✗ 所以那个 catch 从来没生效过，真正的异常直接飞了出去。

② `ensureSession()` 在建不出会话时**抛 `IllegalStateException`**，
   而它在**认证协程里**被直接调用 → 协程中断 → **整条 WebSocket 关闭** →
   手机无限重连 = 「重连中」 ✗

**改法**：

- `run()` 兜住 `start()` 的异常，统一变成「退出码 -1 + 原因」，
  让上层本来就有的 `exitCode != 0` 分支去处理
- **认证之后的设置阶段全部隔离**：会话/接入层/各种清单推送失败**只降级**，
  不再带走连接 —— 这些是**增强功能**，不是连接的前提

**真机验证**：腾讯云 Linux（无 tmux、无桌面）现在显示 **「已连接 · pty」** ✓

### 顺带说明一个仍然存在的限制

**没有 tmux 时终端是空白的** —— 因为会话建不出来，没有东西可显示。
这本身是合理的（没 tmux 就是没有会话管理），
但**手机端还没说清原因**，只给了块空白 —— 跟"无桌面"是同一类问题，
下一步补。

---

## [0.1.43] — 2026-09-20
### 无桌面的执行端：**说清为什么看不到**，而不是给一块空白

服务器上没有图形界面，所以截图和屏幕画面**本来就拿不到**。
以前手机端会画一块空白 ✗ —— 用户看到的是**一个坏掉的功能**，
而真相是**一个不存在的功能**。这两者该给的反馈完全不同：
前者让人去修，后者让人换用别的视图。

现在执行端会把自己的能力报上来（`capabilities.desktop`），手机端据此：

| 位置 | 表现 |
|---|---|
| **屏幕视图** | 说明面板：没有图形界面 / 截图、屏幕画面看不到 / 终端、命令、会话照常能用 / 想看桌面就给它装一个 |
| **截图按钮** | 点一下直接说清，**不发一个注定失败的请求** ✗ |
| **服务端启动** | 顺便讲清少了什么、不影响什么 |

> **实测**（模拟器 + 本机强制 headless 的执行端）：三处都验过。

### 两个实现上的坑

**① `GraphicsEnvironment.isHeadless()` 是惰性的** ✗
它在真正碰 Toolkit 之前经常返回 `false`，哪怕这台机器根本没有图形界面。
第一版拿它当"有没有桌面"，于是**服务端报了 `desktop=true`**，
手机端以为有桌面，仍然画空白 ✗。现在以 **warmup 的真实结果**为准。

**② 窄栏里的长句子会被切掉** ✗
「左右」布局下那块面板只占半屏宽，我第一版写了三句完整的话，
**左右两头都被切没了**（截图里看到的是"是 Linux 服务器，没有图形界面，看…"）。
现在拆成**一行一个短句**，每行都能在窄栏里放下。

---

## [0.1.42] — 2026-09-20
### 三种地址都会自动认（含刚补上的 Tailscale IPv6）

| 类型 | 从哪来 | 实测 |
|---|---|---|
| **Tailscale** | 网卡（v4 段 `100.64.x.x/10`，v6 段 `fd7a:115c:a1e0::/48`） | ✓ |
| **局域网** | 网卡 | ✓ |
| **公网** | **问外面**（网卡上根本没有，云服务器是 NAT 的） | ✓ |

> 补了一处：以前**只看 IPv4** ✗ —— 而有些环境只有 Tailscale 的 v6 地址，
> 那种机器会被判成"没有 Tailscale"。

**但 IPv6 不进连接卡**：`fd7a:115c:a1e0::a037:8852` 有 26 个字符，
让人在手机上抄这个不现实 —— **连接的可行不等于交互的可用**。
只有在"一个可用的 v4 都没有"时才会单独提一句。

### 无图形界面时不再是"像故障"的报错

以前服务器上启动会打：

```
[screenshot] warmup failed: java.awt.AWTException: headless environment
```

**看着像装坏了** ✗。现在说清是什么、影响什么、不影响什么：

```
[daemon] 这台机器没有图形界面（服务器）：
[daemon]   截图 / 屏幕画面功能不可用 —— 点它会提示这个原因；
[daemon]   终端、命令、会话、审批都不受影响，照常能用。
```

### 终端输出里的 markdown 星号清干净了

`**会话管理用不了**` 在终端里会**原样显示星号** ✗ ——
这跟界面上那个坑是同一个（Kuikly 的 Text 不解析 markdown），
只是这次的输出目标是**终端**。全仓扫过一遍。

---

## [0.1.41] — 2026-09-20
### ⚠️ 安全闸自己成了泄漏源（用户发现的）

用户问："安全闸本身 git 跟踪吗？不然这样也泄露。"

**他说对了，而且比他想的更糟。**

那道闸的规则表 `scripts/sensitive-patterns.txt` **被 git 跟踪**，
里面逐条写着本项目的真实地址：Tailscale 的、局域网的、服务器的公网。
**闸本来是用来防泄漏的，结果它自己成了泄漏源。**

而且这个设计**必然漏**：每来一个新地址都得手动加一条。
实测就漏了一次 —— 一个真实公网地址随 CHANGELOG 发了出去，
是**发现之后**才补进规则表的。

**现在改成通用检查**：闸里扫出所有 IPv4，只要不在"文档示例段"里就拦。
规则写在代码里，**规则表不再含任何真实地址**。

允许出现的只有：

| 段 | 来源 |
|---|---|
| `192.0.2.x` / `198.51.100.x` / `203.0.113.x` | RFC 5737 文档专用 |
| `192.168.1.100` / `10.0.0.1` | 常见示例写法 |
| `100.x.x.x`（字面量 x） | 占位写法，不是地址 |

**宁可误报让人来确认，也不能漏。** 加完之后立刻抓到两处：
一处是我注入的测试值，另一处是**文档里我自己写的占位 IP**
（虽然是占位，但闸分不出来 —— 这正是它该有的严格程度）。

> 顺带把文档里所有具体地址改成示例写法。

---

## [0.1.40] — 2026-09-20
### ⚠️ 云服务器的公网地址**根本查不到**（用户发现的）

用户问："查询公网的代码有问题？这台设备好像是 1. 开头的一个公网。"

**他说对了。** 第一版的公网地址是**扫网卡**找的 —— 而**云服务器的公网 IP
不在网卡上**：腾讯云 / AWS / 阿里云给你的公网地址是 **NAT 映射**的，
网卡上只有内网地址。

实测：一台腾讯云机器网卡上是 `10.0.0.12`，公网是 `203.0.113.10`。
于是它的连接卡里**没有公网那一行** —— 而"能公网直连"恰恰是云服务器
相对家用电脑最大的优势，**正是最该显示的那条**。

现在改成**问外面**（`api.ipify.org` 等，总预算 4 秒，拿不到就少一行，
不阻塞启动）。实测：

```
公网      203.0.113.10:18082
局域网    10.0.0.12:18082
```

### 地址顺序改成「能用范围从大到小」

顺带修了另一个错：卡片第一行给的是**局域网**地址，而云服务器的
内网地址对手机**根本不可达** —— 它是唯一"看起来像本机地址"的那条，
用户会先试它，然后以为坏了。

| 顺序 | 说明 |
|---|---|
| **Tailscale** | 任何网络可用，无需配置 |
| **公网** | 任何网络可用，但端口要放行 |
| **局域网** | 同一个 Wi-Fi 时最快（**排最后**） |

每条都带一句说明，用户不用理解原理，照着从上往下试就行。

---

## [0.1.39] — 2026-09-20
### 启动时给一张**能照抄的连接卡**

地址和码以前散在三处（自检里、二维码上方、永久码横幅里），
用户要自己拼起来才能连。现在启动就打一张卡：

```
  ╔════════════════════════════════════════════════════════════╗
  ║ 用手机连这个                                               ║
  ╠════════════════════════════════════════════════════════════╣
  ║ 地址（从上往下试，上面那条最可能通）                       ║
  ║   Tailscale 100.x.x.x:8080 ←先用这个                       ║
  ║   局域网    192.168.1.100:8080                             ║
  ╠════════════════════════════════════════════════════════════╣
  ║ 连接码                                                     ║
  ║   临时  8195      3 分钟，连一次就换                       ║
  ║   永久  ikffA_    按 d 可删掉重生成                        ║
  ╚════════════════════════════════════════════════════════════╝

  新设备首次连接时，需要在这个终端按 y 同意
  （无终端就 echo y > ~/.aircontrol/approve）。
```

顺序是**从最可能可用到最不可能**（Tailscale → 局域网 → 公网），
用户不需要理解为什么，从上往下试就行。

> 顺带修了个对齐问题：**中文是双宽字符**，用 `padEnd` 按字符数补空格，
> 框线是歪的（第一版就是歪的）。改成按**显示宽度**算。

### App 端：连上之前明确告诉用户「去执行端授权」

新设备配对要服务端点头，而手机这边**必须说清"现在该去做什么"**。

以前会把执行端那句原始错误抛给用户 —— 他看到的是"连不上"，
会以为是自己地址填错了，然后**反复改地址** ✗

现在：

> 执行端还没有确认这台设备。
>
> 去执行端那台机器上：
>   · 能看终端 → 你会看到一行「设备「…」想连接」，按 y 回车同意；
>   · 没有可交互的终端 → 在任意 SSH 会话里执行：
>         echo y > ~/.aircontrol/approve
>
> 同意之后再点一次「连接并保存」就行。

也加了「试得太频繁（限速）」的对应文案。

---

## [0.1.38] — 2026-09-20
### 启动时做一次**环境自检** —— 别让用户撞墙了才知道

实测里最常见的三种"就是连不上"，**本来都能在启动时判断出来**：

1. **手机在 4G/5G，电脑在家里的内网** —— 两边根本不在一个网络，
   再怎么试地址都没用。用户不知道，只会以为软件坏了。
2. **没有 tmux** —— 能连、能用单会话，但会话管理用不了。
   不预先说，用户点了「新建会话」看到 `E004` 会当成 bug。
3. **手机没装 Tailscale** —— 电脑装了、手机没装，照样连不上。

现在启动时打一份自检：

```
[daemon] ── 环境自检 ─────────────────────────────────────
[daemon] ✓ tmux 已装 —— 会话管理可用
[daemon] 检测到 Tailscale 地址 100.x.x.x —— 手机也用这个地址最稳。
[daemon] 手机端也要装 Tailscale 并登录同一个账号，否则连不上。
[daemon] 如果手机和这台机器在同一个 Wi-Fi，也可以用局域网地址 192.168.1.100（通常更快）。

[daemon] 看本机地址的命令：
[daemon]   macOS:   ipconfig getifaddr en0
[daemon]   Linux:   ip -4 addr show | grep inet
[daemon]   Windows: ipconfig | findstr IPv4
[daemon] ─────────────────────────────────────────────────
```

缺 tmux 时会说清**用不了什么**（不是只报一句"缺个东西"）和三个平台的安装命令。

### 三种网络环境给三种建议

| 检测到 | 说什么 |
|---|---|
| **Tailscale 地址**（100.x.x.x/10） | 手机也用这个最稳；**手机端也要装** |
| **公网地址** | 能不能连还看端口转发/安全组，不确定就先走局域网或 Tailscale |
| **只有内网** | 同 Wi-Fi 能连；手机在外网就**连不上**，这不是软件的问题 |

### 非交互模式**绝不提问**

`nohup` / 管道 / CI / **AI 执行** 下 `System.console()` 是 null，这时**只报告**。

**非交互却去问问题是最糟的** —— 没人回答，程序会卡在那儿。

> 新增 6 个用例（总数 127 → **133**），每条都对应一个真实踩过的坑：
> 建议里必须有查地址的命令、"同一个 Wi-Fi"这个前提必须点明、
> 内网/Tailscale 段分类正确、**非交互输出里不能出现问句**、
> 缺 tmux 时必须给出三个平台的安装命令。

---

## [0.1.37] — 2026-09-20
### 新设备拿对码也要**服务端点一下同意**（像蓝牙配对）

上一版做了永久码，靠限速兜着。**这是不够的**——限速只是把爆破成本抬上去，
不是挡住。用户提了一个更好的做法：**码不是通行证，人点的那一下才是。**

现在：新设备用对码连上来，执行端终端会问：

```
[daemon] ──────────────────────────────────────────────
[daemon] 设备「aircontrol-192.168.1.100:8080」想连接（来自 mac）。
[daemon] 如果就是你现在正在连的这台，按 y 回车允许；
[daemon] 不认识它就按 n 回车拒绝（也可以不理，60 秒后自动拒绝）。
[daemon] ──────────────────────────────────────────────
```

没点同意，什么都进不来。**于是永久码被爆破也没用** ✗

| | 只有对码 | 加批准后 |
|---|---|---|
| 码泄露 | 直接进 ✗ | 只触发一次询问 ✓ |
| 爆破 | 靠限速硬扛 | **爆破成功也没用** ✓ |
| 限速的角色 | 唯一防线 | 退化成纵深防御 ✓ |

**已信任过的设备不走这一套**——它们用设备令牌，重连无感。
否则每次重连都要跑回电脑前按 y，那没法用。

### ⚠️ 顺带堵掉一个缺口

我一开始只给 `/ws` 加了审批，**`/term` 和 `/screen` 还能直接用永久码进去** ✗ ——
攻击者拿爆破来的永久码虽然进不了 `/ws`，却能从这里拿到**终端字节流和屏幕画面**。

现在这两个通道**只认设备令牌和临时码**：临时码本身就是"当面给、一次即废"的，
等价于已经确认过；永久码必须先在 `/ws` 上过审批换成令牌。

### 几个刻意的设计选择

- **同时只允许一个待批**：两个请求同时在等，终端上先后打两行"允许吗"，
  用户分不清自己的 `y` 回答的是谁——**分辨不清的确认比没有确认更危险**，
  所以后来的直接拒掉，不让它排队。
- **超时返回 `null` 而不是 `false`**：日志里要能区分"没人管"和"明确拒绝"。
- **连接断了算作废（`null`），不算拒绝**：网络抖动不该被记成用户拒绝。

> 新增 6 个用例（**121** 全绿），每条对应的都是安全边界：
> 同意/拒绝/超时三态、重复敲 y 幂等、并发只批一个、作废≠拒绝、没人等时敲 y 无副作用。

---


### 永久码改成**混合字符**（原来 6 位纯数字太小了）

上一版做成 6 位纯数字 —— **这是个错误**：

| 字母表 | 空间 | 相对 |
|---|---|---|
| 10 位数字 | 10^6 = **100 万** | 1× |
| 62 位（大小写+数字+符号） | 62^6 ≈ **570 亿** | **5.7 万倍** |

100 万的空间，局域网里每秒试几千次，**几小时就枚举完了**。
当时我的应对是加限速——但那只是**把成本抬上去，不是把空间做大**。

现在用 **62 个字符**：大小写字母 + 数字 + 少量符号，
**去掉了容易看错的** `0`/`O`/`o` 和 `1`/`l`/`I` ——
码是要肉眼抄到手机上的，认错一个字符就等于连不上。

熵做大之后，限速从「唯一防线」退化成**纵深防御**。

⚠️ 由此多了一个必须传达的约束：**码区分大小写**。
手机输入法默认首字母大写、还可能自动纠正，所以启动横幅里写明了。

### 新设备配对要**服务端点一下同意**（像蓝牙配对）

用户提的：**码不是通行证，人点的那一下才是。**

新设备用对码连上来，执行端终端会问：

```
[daemon] 设备「aircontrol-192.168.1.100:8080」想连接（来自 mac）。
[daemon] 如果就是你现在正在连的这台，按 y 回车允许；
[daemon] 不认识它就按 n 回车拒绝（也可以不理，60 秒后自动拒绝）。
```

没点同意，什么都进不来。**于是永久码即使被爆破也没用。**

**已信任过的设备不走这一套** —— 它们用设备令牌，重连无感。
否则每次重连都要跑回电脑前按 y，那没法用。

### ⚠️ 顺带堵掉一个我自己造的缺口

一开始只给 `/ws` 加了审批，**`/term` 和 `/screen` 还能直接用永久码进去** ——
攻击者拿爆破来的永久码虽进不了 `/ws`，却能从这里拿到终端字节流和屏幕画面。

现在这两个通道**只认设备令牌和临时码**。

### 几个刻意的设计选择

- **同时只允许一个待批**：两行"允许吗"同时在等，用户分不清自己的 `y` 回答谁 ——
  **分辨不清的确认比没有确认更危险**，所以后来的直接拒掉，不排队。
- **超时返回 `null` 而不是 `false`**：日志要能区分"没人管"和"明确拒绝"。
- **连接断了算作废（`null`），不算拒绝**：网络抖动不该被记成用户拒绝。

> 新增 15 个用例（永久码 9 + 审批 6），总数 101 → **124**，全绿。

---

## [0.1.36] — 2026-09-20
### 永久连接码（6 位）+ 认证限速

临时码（4 位、3 分钟、用一次就换）对**陌生的一次性连接**是对的，
但对**你自己的手机**太折腾——开发这一轮我因为"码过期了"来回跑了五六趟。

现在多一个**永久连接码**：

```
[daemon] 永久连接码：226728
[daemon]   你自己的手机用这个，不用赶 3 分钟。
[daemon]   它只存哈希、不存明文——错过这次就得换一个，请现在记下。
[daemon]   换一个：在执行端按 d 回车（旧的立刻作废）。
```

**说清楚它危险在哪，以及怎么防：**

6 位数字只有 **100 万种组合**。局域网里每秒能试几千次——
**不限速的永久码等于没锁。** 所以这次同时加了**认证限速**：

| 连续失败 | 锁多久 |
|---|---|
| 5 次 | 30 秒 |
| 10 次 | 5 分钟 |
| 20 次 | 1 小时 |

指数退避，**成功一次就清零**（用户试错几次终于输对，不该还背着之前的账）。
按来源分别计数，一个人被锁不连坐别人。

实测：连输 6 次错码 → 第 6 次返回「被限速（30 秒）」✓

**存储**：只存 SHA-256，不存明文；文件权限 `0600`。
但要说清它的边界——**哈希存的是"文件被别的程序读到"这件事，
不防"6 位空间太小被暴力枚举"**。后者只能靠限速。

**它跟设备令牌的关系**：设备令牌（32 字节随机、哈希存储、可撤销）**本来就更好**，
配对过一次之后重连根本不需要任何码。永久码是给"手输比扫码方便"这个场景的补充，
不是替代。

**终端按键**（以前只有"回车换码"，没人知道还能敲什么）：

```
回车 / r = 换一个临时连接码
d        = 删掉永久连接码并换一个新的
h        = 显示帮助
```

> 新增 14 个用例（永久码 6 + 限速 8），**全部围绕安全属性**：
> 明文不落盘、删除后旧的立刻失效、长度不对直接拒、
> 前四次失败不锁、锁会过期、成功清零、不连坐、表不会无限长。
>
> 用例总数 101 → **115**，全绿。

---

## [0.1.35] — 2026-09-20
### 点「打开 Codex」像没反应 —— 两个原因叠在一起

实测：点「打开 Gemini CLI」，执行端那边**确实起了新窗口**（tmux 里能看到），
但**手机屏幕上什么都没有**。用户的心智模型是"我点了，就该看到它"，
没动静 = 坏了 = 再点一次（于是又开一个窗口）。

**原因一：该推的状态没推。**

`openBrain` 成功后只调了 `pushSessions()`，**没推 brains** ——
而 `brains` 里带着 `current`（现在跑的是哪个 CLI）。
于是执行端明明起了 Codex，手机上"哪个在跑"**永远不更新**。

**原因二：那几秒里一个字都没说。**

CLI 自己要几秒才画出界面，而这段时间应用没有任何反馈。

现在：

- 芯片有**三种明确状态**：正在跑（琥珀底+琥珀字）／正在打开（中灰底 + 文字变
  「打开中」）／可点（描边底）
- 点下去立刻显示「正在打开 X…」，收到执行端回报后变「已打开 X（新窗口）」
- "正在打开"期间**挡住连点**

实测：点 Codex 后 **1 秒内**芯片变琥珀色 ✓

> 顺带把芯片里硬编码的两个琥珀值收进 token（`brainActive` / `brainActiveText`）。

---

## [0.1.34] — 2026-09-20
### 真机连着用，发现两个静态检查看不见的问题

**① 发完命令，输入框里的字不清 → 再点一次就重复执行**

实测：输入 `pwd` 点「发送」，命令**执行了**（终端返回了路径），
但输入框里 `pwd` **还在** —— 再点一次「发送」就会**再执行一遍**。
对 `pwd` 无所谓，对 `rm` 是灾难。

根因：Kuikly 的 `Input` **不会因为状态变量变化而同步原生 EditText**，
所以 `command = ""` 只改了状态、框里那串字还在。

> 讽刺的是，**我自己在同一文件里写过这条注释**（"故意不清空输入框：
> Kuikly 的 Input 不会同步外部改值"）—— 知道限制，却在另一处依赖了它。

现在发完显式 `commandInputRef?.view?.setText("")` 推一次空串。

**② 三个「视图」选项，看起来有两个是选中的**

```
视图  [终端]  [屏幕]  [截图]
       ↑蓝     ↑灰    ↑棕
```

每个选项带自己的强调色（终端 `#2F6FEB`、屏幕绿、截图棕）。问题在于
**「终端」那个蓝正好是主操作蓝** —— 它看起来永远像个被按下的主按钮；
而三个色各自都像"选中"，于是"当前到底是哪个视图"要靠猜。

现在**选中样式只有一种**：选中 = 主操作蓝，未选中 = 中灰。
这一条独立评审也提过（"选中态和主操作同为 #2F6FEB，眼睛落在哪都一样重"）。

### 实测通过的核心链路

```
user@mac aircontrol % echo hello-from-phone
hello-from-phone
user@mac aircontrol % pwd
/path/to/aircontrol
user@mac aircontrol % whoami
user
```

手机发命令 → 执行端执行 → 输出回到手机 ✓ 发完输入框自动清空 ✓

---

## [0.1.33] — 2026-09-20
### ⚠️ 终端一直空白：一个字符串转义泄漏

**症状**：手机显示「已连接」，但终端区**永远是空的**，执行端日志里
**一条 `/term` 都没有**。

**根因**：源码里写着

```kotlin
token(loadDeviceToken("${'$'}{ctx.activeHost}:${'$'}{ctx.activePort}"))
```

那个 `${'$'}` 是我之前用脚本改代码时把 **Kotlin 的转义写法**当普通文本写进去了 ✗
运行时它是**字面量** `"${ctx.activeHost}:${ctx.activePort}"`——**根本没插值**。

于是取设备令牌时查的是一个不存在的 key，永远拿到空串；
而连接码又已经被 `/ws` 认证**用掉了**（一次性），`/term` 手里什么都没有 → 连不上。

**改法**：把那 4 处泄漏修掉；顺带让 `host/port/pin/deviceId/token`
**每个属性 setter 都触发一次连接尝试**（原来只有 `pin` 触发——
如果框架传属性的顺序变了，这一次会因 host 还没到而早退，之后再没人叫它）。

### 实测：核心链路通了

```
user@mac aircontrol % echo hello-from-phone
hello-from-phone
user@mac aircontrol %
```

手机发命令 → 执行端执行 → 输出回到手机 ✓

> 这两个 bug（转义泄漏 + 只有 pin 触发）**编译都通过、101 个用例全绿**。
> 是**真机连着用**才暴露出来的——静态检查看不见"连上了但什么都不显示"。

---

## [0.1.32] — 2026-09-19
### 独立评审第二轮的结论：最高杠杆的两条，都是"默认值选错了"

**① 连接码输入框默认填着 `1234`**

一个**看起来像真的** 4 位数预填在框里。新人会以为那就是他的连接码，
点「连接并保存」必然失败——而错误提示（"连接码不对或已失效"）
**跟屏幕上显示的东西自相矛盾**。

> 这跟"地址框宁可留空、也不用真实 IP 当占位符"是**同一条原则**：
> **不要把任何看起来像真数据的东西放进输入框。** 提示进 placeholder，值就该是空的。

**② 三种连接方式里，默认选了最难的那条**

`IP 连接` 要用户从**另一块屏幕**抄两个值，其中一个还是 3 分钟就过期的秘密；
`扫码连接` 一次拿到地址+端口+连接码，什么都不用抄。

列表第一个放的是扫码（最容易），默认却是 IP（最难）——**位置和默认自相矛盾**。

现在：**没有连接历史时默认走扫码**；老用户沿用上次用的方式。

### 按评审修的其余几条

- **「关闭」有了按钮待遇**：它原来是一行灰字，旁边四个同类操作
  （展开/收起、检查更新、复制）都有灰底按钮。而且整行都挂过 click——
  点标题或中间空白也会关掉设置、**丢掉滚动位置**。现在热区只限按钮。
- **下载地址行不再挤在一起**：GitHub 那行原来只剩 **4px** 余量（Gitee 有 132px）。
  现在链接最多两行、按钮有左间距——窄屏/大字体下也不会被截。
- **3 分钟提示挪进表单**：它原来只在列表视图里，**恰好在你输码时消失**。
- **失败文案补上"下一步"**：原来只诊断（"连接码不对或已失效"），
  没说怎么恢复。现在说清去执行端窗口按回车换一个、或直接扫码。

---

## [0.1.31] — 2026-09-19
### 一次独立评审揪出来的问题（含一个我自己引入的无障碍倒退）

这轮做了一次**独立评审**（不是我自己看）：把渲染图交给一个没参与实现的评审者，
按 Nielsen 十项 + 对比度实测打分。它揪出几件我自己没看见的事。

**① 白字压绿底 = 2.54:1，WCAG AA 都不过（还连大字 3:1 都不到）**

上一轮做设计系统时，我把所有绿色统一成了一个 `success`（#3FB950）——
那是**状态点**的绿，很亮。拿它当按钮底色，白字压上去只有 **2.54:1**。
而迁移前一直在用的 #2E7D32 是 **5.13:1** ✓

**我那次"统一"反而把无障碍改差了。** 现在分成两个 token：

| token | 用途 | 白字对比 |
|---|---|---|
| `success` #3FB950 | 状态点、文字 | （不作底） |
| `successFill` #2E7D32 | 正向按钮底色 | **5.13:1** ✓ |

**② 说明文字 4.10:1，11sp 下不过 AA**

`textFaint`（#6B7683）压在背景上只有 4.10:1。它现在**只用于占位符**
（一填字就消失的那种），正文说明一律用 `textDim`（7.73:1）。

**③ 注释说"在折叠之外"，代码却在折叠里面**

「执行端：未连接」那条状态，注释明明写着*"注意它在折叠之外，任何时候都该看得见"*，
代码却把它放进了 `vif({ connSectionOpen })` 里——**收起时完全看不见**，
而收起恰恰是它的默认状态。

> **注释说一套、代码做一套，比没有注释更坏**——它会让 review 的人放心地跳过。

现在真的搬到折叠之外了，而且「版本」区里那份重复的也删了。

**④ 收起折叠会把表单变成孤儿**

表单的字段渲染条件是 `newConnOpen && mode`，跟 `connSectionOpen` 无关。
只收折叠区的话，地址/端口/连接码会**孤儿一样**留在「版本」上面，
没有标题也没有模式选择。现在收起时会一并关掉表单。

**⑤ 两个「取消」按钮**（上个版本新加时忘了删旧的）

**⑥** 又一条状态重复：`已连接 · host:port` + 自动追加的 integration。

### 评审明确说"别动"的地方

它点名了三处做得好的，让我别改坏：破坏性操作的配色（删除 = 深红底+浅红字，
"醒目但不吵"）、代价高的动作都有诚实文案（「连接并保存」写明副作用、
飞行中变「连接中…」、断开确认解释令牌会被撤销）、以及连接历史行的组件复用。
这些保留。

---

## [0.1.30] — 2026-09-19
### 顶栏「会话」只留图标

抽屉里有自己的标题和「设置」入口，顶栏再写一遍"会话"两个字是重复的；
而且顶栏每多两个字，下面终端就少一点横向空间。
汉堡图标本身就是"打开侧栏"的通用约定，不需要文字解释。

### 连接失败的原因，归到**对应的那条记录**里

以前失败原因会出现在**顶栏**，又长又看不懂：

```
接收失败: Failed to parse HTTP response: the server prematurely closed the connection
```

而且**它本来就是某一条连接的事**，不该占着全屏的状态位。

现在：

- **顶栏只说当前状态**，一句话：`未连接` / `连接中…` / `已连接 · pty`
- **原因落在对应的历史行里**，固定格式：

```
连接历史
  ● 192.168.1.100:8080                    [重新连接] [断开]
    连接失败：连接码不对或已失效（3 分钟过期、用过一次也作废）
```

### 顺带修

- **终端通道的状态不再覆盖连接状态**。以前 `终端已断开` 会把顶栏占掉——
  那是两件事：顶栏说"我和执行端通不通"，通道说"终端字节流通不通"。
- **「没有连接任何服务端」不再跟上方矛盾**：列表里明明有一条失败记录，
  底下还说"没有连接任何服务端"。现在只在真的什么都没有时才出现。

---

## [0.1.29] — 2026-09-19
### 断开连接现在会先问一句

**断开是不可逆的**——它会撤销这台机器的授权，之后必须回执行端终端取一个**新的连接码**
才能再连。误触一下就得跑一趟电脑，代价太大。

现在点「断开」会弹：

```
断开连接？
断开后本机的授权会被撤销——下次连接需要去执行端那个终端窗口
取一个新的连接码。
（如果只是想重连，点「重新连接」就好，那个不影响授权。）
                              [取消] [断开]
```

**判断标准是「能不能撤销」**：断开 / 删除 / 清空这类要确认；
复制 / 展开这类可逆的**不加**——那会变成打断。

同一个确认框也用在了「删除连接记录」和「清空全部记录」上。

### 顺带：发布闸加了「UI 文案检查」

Kuikly 的 `Text` **不解析 markdown**——写 `**加粗**` 会原样显示成 `**加粗**`。
这个坑本项目**犯过三次**（每次都是截图里才看见，代码里完全正常），
所以放进发布闸：发之前扫一遍 Kotlin 字符串里的 markdown 星号，命中就中止。

阳性对照验过（故意塞一个 → 拦住，退出码 1）。

---

## [0.1.28] — 2026-09-19
### 首次使用：给一条能走的路

以前第一次打开这个应用，首页只有一句「没有连接任何服务端」——
**而设置在哪他根本不知道**（抽屉里那个入口对新人来说不可见）。等于把人扔在原地。

现在首次使用（没有任何连接记录）看到的是：

```
还没有任何连接配置
首次使用需要先连上你的执行端（那台跑着 AI 的电脑）。
[去设置连接 →]
```

点进去会**直接打开设置、并自动展开「连接服务端」**，落在「新增连接」上——
不用自己找到那个折叠区再点开。

> 空状态的原则：**说明 + 一个直达下一步的入口**。光说明不给出路，等于没做。

### 顺带修

- **「取消」按钮跑到表单上面去了**：它原来跟标题写在一起，
  于是按钮出现在"新增连接"和表单**之间**——要取消的东西在它下面，视觉上说不通。
  现在放在三个模式的表单之后。
- 界面文案里剩下的符号（`✕` `⚙` `☰` `▴▾`）能换图标/文字的换了：
  规范里**「零 emoji」是硬规则**，而齿轮在 16px 下**画不准**（试过滑杆，糊）——
  **能画准的简单图形 > 画不准的精美图形**，画不准就用纯文字。

---

## [0.1.27] — 2026-09-19
### 修：点「连接并保存」像没点上；失败了只报一句看不懂的英文

两件事，都跟"用户不知道自己点没点上"有关。

**① 连接弹窗被设置页盖住了。** 弹窗原来画在设置页**前面**，
于是在表单里点「连接并保存」之后，弹窗出现在**设置页底下**——
屏幕上什么变化都没有。现在弹窗排在所有浮层之后（最上层）。

**② 按钮自己也有状态了。** 弹窗在屏幕中央，而用户的目光在按钮上。
现在按下后按钮直接变成「**连接中…**」并变灰——不用去别处找反馈。

**③ 失败时说的是人话，不是异常。** 以前会弹：

```
连接失败: Failed to parse HTTP response: the server prematurely closed the connection
```

真实原因是**连接码过期了**。这句英文对用户没有任何信息量，
只会让人以为"这软件坏了"。

根因是执行端发完错误帧**立刻关连接**，客户端还没读到那一帧连接就断了。
现在发完等一下再关；客户端也**不再把原始异常直接抛给用户**，
而是翻译成能照着做的话：

> 连接码不对，或者已经失效。
>
> 连接码 3 分钟自动换一次，用过一次也立刻作废。
> 去看你启动执行端的那个终端窗口——最新那串就显示在里面
> （图省事就直接扫它上面的二维码）。在那个窗口里按一下回车可以立刻换一个新的。

**④ 失败快多了。** 实测 7 秒就报出来（以前要干等 12 秒的看门狗）。

### 顺带

清掉界面文案里漏出的 markdown 星号（Kuikly 的 `Text` 不解析 markdown，
`**加粗**` 会原样显示成 `**加粗**`）。

---

## [0.1.26] — 2026-09-19
### 立了设计语言，界面不再是一堆随手挑的值

动手前先量了一下现状：

| | 之前 |
|---|---|
| 硬编码颜色 | **47 种** |
| 字号 | **7 级**（10/11/12/13/14/15/16，中间四级挤在一起） |
| 圆角 | 4 种 |

那不是"设计得不够好"，是**根本没有设计**。

现在 `app/.../design/Ds.kt` 是唯一的取值来源，三层 token
（组件 → 语义 → 原始），代码里只允许出现**语义层**。取向上它是个**工具**：

- 深色为主（长时间盯终端）；底色**不用纯黑**（OLED 上对比过硬），用带蓝的深灰
- **颜色克制**：只有「状态」和「主操作」配有色，其余全中性灰
- 字号按 Major Third 级数，**只留五级**
- 间距 4 的倍数；圆角三档；控件高度统一

**迁移结果：47 种颜色 → 9 种**（161 处替换）。

### 图标自己画，不引任何资源

`design/Icons.kt`：菜单 / 更多 / 键盘 / 垃圾桶 / 复制 / 加号 / 展开 / 状态点，
用 `View` 矩形拼，**一份代码三端同形**（Kuikly 跨 Android/iOS/鸿蒙，
图标字体各端要配资源、位图要出好几套）。

也顺带满足了设计规范里的**「零 emoji」硬规则**——emoji 是机器生成感最强的痕迹，
而且各平台字形差异极大。状态点改成画出来的圆点，语义交给旁边的文字。

### 首页：一屏只有一个主角

以前连着的时候，**「连接历史」占掉首屏三分之一**，而终端被挤在中间。
那是层次问题：终端才是这个应用存在的理由，历史是低频信息
（抽屉里另有一份）。

现在**连接历史只在未连接时占首屏**；连上之后整屏让给终端。

### 顺带修掉两个真 bug（都是盯着渲染结果才发现的）

1. **终端反复重连**：20 秒内 `/term attach` 12 次。
   根因是 `attr{}` 会随响应式字段变化反复重跑，每次都设一遍连接参数，
   而"正在连"这个状态没被挡住——一次连接还没落地就再开第二条，旧的没人关。
2. **`pin.isBlank() → return` 让终端永远连不上**：那是只有连接码的年代的判断。
   现在配对过的机器重连时 PIN 是空的、靠设备令牌认证，这条判断把它挡死了。

---

## [0.1.25] — 2026-09-19
### 修：设置页里有两个「+ 新建连接」

上一个版本重排设置页时，旧结构的一段「新建连接」没删干净，
于是「连接服务端」展开后会出现**两个一模一样的按钮**。

### 发布流程最后会打印连接码

发完还得自己去翻日志找码，那这个发布就没闭环。现在 `scripts/publish-public.sh`
跑完会顺手把本地执行端换成刚发布的版本，并打出：

```
  ┌────────────────────────────────────────────────┐
  │  连接码    1234                                 │
  ├────────────────────────────────────────────────┤
  │  局域网     192.168.1.100:8080                  │
  │  Tailscale  100.x.x.x:8080                      │
  └────────────────────────────────────────────────┘
```

CI 里自动跳过（那边没有本地执行端）；本地也可以 `AIRCONTROL_NO_SERVE=1` 关掉。

---

## [0.1.24] — 2026-09-19
### 设置页重新分区：每块只回答一个问题

以前「关于」里堆了版本号、下载地址、数据怎么走、连接码提示——**四件事挤在一个标题下面**，
用户找哪一件都得先把这一坨读完。现在各归各位：

| 区 | 只回答 |
|---|---|
| **连接服务端** | 我连过哪些？怎么再连一台？连接码是什么、注意什么 |
| **版本** | 现在什么版本？怎么更新？**去哪下载**（Gitee + GitHub 两个源） |
| **安全** | 我的数据经过谁？（不经任何其他端） |
| **关于** | **这是个什么应用**——一句话 |

- **下载地址从「关于」挪到「版本」**：它是"去哪拿版本"的问题，跟版本号是一件事
- **同时给 Gitee 和 GitHub**：国内 Gitee 通得快，GitHub 是国际通用入口。
  只给一个的话，另一半用户会觉得"这是个只在国内发的野包"。各自带复制按钮。
- **「连接码是进门的钥匙」挪到「连接服务端」**：它是"怎么连"的一部分；
  放在「关于」里，用户找连接时根本看不到
- **「数据不经过其他端」挪到新的「安全」区**：那是隐私声明，不是"关于我们"
- **「关于」改成一句话**：`AirControl：把手机变成你电脑的屏幕和键盘，让 AI 在你的机器上干活。`

### 去掉界面里的 emoji

设计规范里「零 emoji」是硬规则——emoji 是机器生成感最强的痕迹，
而且**各平台字形差异大**（`❗` 在 Android 上是个彩色方框，看着像贴纸）。

连接状态原来用 `❗` 表示"断开"，现在改成**画出来的圆点**：
四种状态同一个形状、不同颜色，语义交给旁边的文字标签。

---

## [0.1.23] — 2026-09-19
### ⚠️ **只有连上了才写进连接历史**

以前是**点「连接」就存**——地址打错、连接码填错，历史里照样多出一条。
用久了列表里全是"从没连上过"的记录，反而找不到真正在用的那台。

现在：

- 表单按钮改叫「**连接并保存**」——说明这一下不只是连，还会记进历史
- **连上了**：写入历史（修改模式则更新原条目）+ 自动关掉表单
- **没连上**：不写历史、**不关表单**（用户要能接着改）

### 首页

- 修掉**两个「+ 新建连接」**：未连接时首页会同时显示两块，
  于是按钮出现了两次。现在列表和按钮全页只有一处。

### 连接历史那一行的三个按钮

- **等宽**了。`✕` 只有一个字符，比「修改」窄一截，排在一起歪歪扭扭。
- `✕` 改成「**删除**」——一个叉号在大列表里既小又难认。

### 设置页「连接服务端」

- **折叠的是整块**（标题 + 内容），不再只收内容留一个孤零零的标题；
  收起后「版本」紧接着上来
- 展开后：**连接历史以列表形式显示**（和首页同一个组件，不再两处两套样式），
  列表下面一个「+ 新建连接」按钮
- 点「+ 新建连接」/「修改」进入**表单视图**，带「取消」退回列表

---

## [0.1.22] — 2026-09-19
### ⚠️ 修：终端一片空白（App 会崩）

`vfor` 用错了：它的体内**必须恰好一个「非条件子节点」**，
在里面直接套 `vif` 会在运行时抛

```
vfor creator闭包内子孩子必须为非条件指令，如vif , vfor
```

→ **App 直接崩**，所以连上之后屏幕中间一大块是空的、终端始终不显示内容。

**这个 bug 编译通过、测试全绿、代码看着没问题**——是**盯着渲染出来的截图**
才发现的（外面包一层普通 `View` 就合规了）。

### ⚠️ 修：`/term`、`/screen` 通道用不了（终端空白）

这两个通道原来用**连接码**认证。而连接码改成**一次性**之后：

1. `/ws` 认证**消费掉**了那串码
2. App 紧接着用**同一个码**去连 `/term` → 被拒

→ 表现成「已连接」但终端一片空白。

现在这两个通道也接受**设备令牌**（和 `/ws` 一致：先令牌、再连接码）。
实测：`[daemon] /term attach（80x24）` ✓

### 说清楚执行端**是什么、在哪看码**

以前文案只说「去看执行端那个终端窗口」——可用户**根本不知道执行端是个命令行程序**，
自然也无从知道"哪个终端"。

现在：

- **`install.sh`** 装完会明说：执行端是命令行程序，跑在终端窗口里，
  连接码就显示在那个窗口；给出**完整的启动命令**；说明"关掉窗口 = 停止执行端"。
- **执行端自己的启动横幅**也会讲一遍身份 + 下次启动的完整命令。
- **App 弹窗/设置页**改成「你启动执行端的那个终端窗口」，并提示可以**直接扫码**、
  **在窗口里按回车换码**。

顺带修掉一个：`install.sh` 还在让用户**自己定 PIN**（`--pin`）——
跟"随机、限时、一次性"的新模型直接矛盾。已去掉这个参数。

### Kuikly 界面的三个机制性坑（已写进项目的 skill）

这次一并固化到 `.dsh/skills/kuikly-ui/`（本地 skill，不进仓）：

1. 响应式只追踪 `attr{}` / `vif{}` **里面**的读取（犯过三次）
2. 动态列表必须用 `vfor`，普通 `for` 不更新（删了不刷新、两处不同步）
3. `vfor` 体内**只能有一个非条件子节点**（套 `vif` 会崩）

外加：浮层要最后画、`flex(1f)` 只 grow 不 shrink、每个动作都要有可见反馈、
错误信息不能指向错误方向。

---

## [0.1.21] — 2026-09-19
### 首页

- **去掉「打开设置」按钮**——那是个"把人踢到另一个页面去"的入口，
  而用户需要的东西（连过哪几台、怎么再连一台）在主页面就能给全。
- **没连上时**首页直接显示：
  - 一行「没有连接任何服务端」
  - **连接历史**（每条带状态）
  - 「+ 新建连接」按钮

### 新增一种状态：**连接错误，原因 xxx**

以前"连过但失败了"和"没连过"长得一样。现在分开了：

| 状态 | 含义 |
|---|---|
| 已连接 | 通着 |
| 连接中 | 正在连 |
| **连接错误：原因** | **试过了，没成功**（并说清为什么） |
| 已断开连接 | 没连过，或连上后断开了 |

失败原因**按条记**（不是全局一个变量）：连 A 失败、再连 B 成功之后，
A 那条仍然标着"上次为什么没连上"——回头看列表时这是有用的信息。
连上之后会自动清掉（失败了又连上了，就该翻篇）。

### 设置页：连接服务端可折叠，两种入口默认不同

| 入口 | 连接区 |
|---|---|
| 首页点「+ 新建连接」/「修改」 | **展开**（用户是来干活的） |
| 抽屉底部的设置齿轮 | **折叠**（多半是来看版本/关于的） |

折叠时给一句摘要（「已记录 3 台服务端」），不然用户不知道里面有什么、
值不值得展开。

> 「执行端：0.1.21 · 协议 1」这一行**在折叠之外**：折叠的是配置项，
> 而"现在通不通"是状态，任何时候都该看得见。

---

## [0.1.20] — 2026-09-19
### ⚠️ 修：「连接码不对」被显示成「连接超时」

实测反馈：拿着连接码连，看到的是 **"连接超时"**，于是去查 Wi-Fi、怀疑网络——
而真实原因是**那串码已经失效了**（3 分钟过期，或者已经用过一次）。

原因：会话那边其实**正确地报了「认证失败」**，但连接弹窗没看它，
一直等到自己那个 12 秒看门狗到点，才统一说「连接超时」。
**错误信息把人往错误的方向引**，比不提示更糟。

现在：会话一失败（认证错 / 版本过旧），弹窗**立刻**切到失败态，并说清是哪一种：

> 连接码不对，或者已经失效。
>
> 连接码 3 分钟就过期、连过一次也会立刻作废——
> 去看执行端那个终端上**最新**的那串（按回车可以立刻换一个）。

### 顺带

连接日志里也记了「已断开」和「认证失败」的区别，排查时不用猜。

---

## [0.1.19] — 2026-09-19
### 修：点「下载新版本」要等好几秒，而且看不出在干什么

两个 bug 叠在一起：

**① 探测下载源时把整个安装包下了一遍。** 代码用的是 `client.get(url)`，
而它**会把响应体缓冲下来**——APK 有 6 MB，于是"测速"实际上把整包下完才返回。
注释里还写着"不读 body，不会把整包拉下来"，是错的。

改成 `prepareRequest(...).execute { }`：拿到响应头就返回，**一个字节正文都不取**，
Ktor 会因此中止这次传输。探测超时也从 7 秒收到 **3 秒**——
宁可用一个稍慢的源，也别让你对着"正在测速"干等。

**② 按钮文字不会变。** 写的是

```kotlin
actionButton(if (ctx.pickingSource) "测速中…" else "下载新版本", ...)
```

参数是在**构建时**算的，而 Kuikly 只追踪 `attr{}` / `vif{}` **里面**的读取——
所以点下去按钮上永远是「下载新版本」，看起来像没点上。
加了接受 lambda 的重载，把它挪进 `attr{}`。

### 防连点

- 测速期间按钮变成「**正在测速…**」并且吞掉后续点击
- 再加 5 秒**冷却期**：测速完到浏览器真正接管之间有个空窗，
  那几秒里用户容易以为没点上而再点一次（于是又测一遍、又开一个标签页）
- 状态栏也会写一句「正在测速，挑最快的下载源…」

> 你猜的没错：**确实是在测哪个源快**（Gitee / GitHub / 执行端并发探测，挑最快的）。
> 只是这个过程原来是"偷偷把整包下了一遍"，所以慢得莫名其妙。

---

## [0.1.18] — 2026-09-19
### 🔑 配对过的手机**重连不用再输连接码**

上一版把连接码做成了随机、3 分钟、一次性——安全了，但带来一个新麻烦：
**网络抖动导致重连时，得跑回电脑前看一眼新码**。那没法用。

所以照行业里的做法补上后半截（Chrome 远程桌面叫「配对」、SSH 是密钥认证、
AnyDesk/TeamViewer 是「授权设备」）：

| | 用途 |
|---|---|
| **连接码**（4 位） | 短、限时、一次性 → **只用来首次配对** |
| **设备令牌**（32 字节随机） | 不可枚举、可撤销 → **用来日常重连** |

首次连上用连接码通过后，执行端发一个**设备令牌**下来，手机存住。
之后重连直接带令牌，**不用再输码**——网络抖动完全无感。

- **令牌绑定设备**：换一台手机就得重新配对
- **令牌存的是哈希**（不是明文）——那个文件就在你机器上
- 执行端会打印「已授权设备」，记录在 `~/.aircontrol/devices.tsv`

### 手工断开 = **撤销授权**

这两件事必须分开：

| | 令牌 | 效果 |
|---|---|---|
| **网络抖动自动重连** | 保留 | 无感重连 |
| **手工点「断开」** | **撤销** | 下次要执行端上的**新连接码** |

断开后会明确提示：
> 已断开，并撤销了本机授权。下次连接要用执行端上的新连接码。

分不清这两者的话，要么重连很烦，要么「断开」形同虚设。

### 设置页：连接历史 与 新增/修改 是**两个互斥的视图**

- **连接历史**只在列表视图里显示；打开「+ 新建连接」后列表收起来
  （叠在一起的话，用户在填地址时下面还挂着一长串列表，既抢注意力又把表单挤下去）
- 表单顶部**明确写清是「新增连接」还是「修改连接」**，修改时还会说明
  「改完点连接会更新原来那条，不会新增」
- 修掉一个重复：连接历史原来在设置页里**渲染了两遍**

---

## [0.1.17] — 2026-09-19
### 🔐 连接码改成**随机 · 3 分钟有效 · 连一次就换**

以前连接码是启动时用 `--pin` 指定的一串**固定**数字（示例里还是 `1234`）。
4 位数只有 1 万种可能，固定不变的话，**同一个 Wi-Fi 或同一个 Tailscale 网络里的
任何人都能暴力枚举进来**；而且一旦被瞄到一眼，就永久有效。

远程桌面软件早就不这么做了。现在照那套改：

| | |
|---|---|
| **随机** | 每次启动/每次换码都是新的 4 位数 |
| **3 分钟有效** | 过期自动换一个新的（屏幕上永远有一串能用的） |
| **连一次就换** | 用掉即作废，同一串不能连第二次 |
| **可手动换** | 在执行端那个终端里**按一下回车**就换 |

所以：**截图发出去的码，几分钟后就是废纸；被人瞄到的那串，你连上一次它也就废了。**

> `--pin 1234` 那条路还留着——**开发和自动化测试要用**，
> 不然每跑一次都得人肉读一遍终端上的随机码。
> 但它**必须显式选择**；不传就是上面那套安全默认值。
> **默认值才是绝大多数人实际用的那个，所以它必须是安全的那一个。**

🔒 比较用定长比较（不是 `==`）：普通的 `==` 在第一个不同的字符处就返回，
理论上能被逐位试出来。随机数用 `SecureRandom` 而不是 `Random`（后者可预测）。

### 「关于」文案

- 「不经过**我们的服务器**」→「不经过**任何其他端**」。
  前一句反而暗示"我们有个服务器"，而后一句才是事实：这条链路上压根没有第三方。
- 补一句：**连接码是进门的钥匙，别把二维码或连接串发出去。**

### 设置页

「执行端 0.1.x · 协议 1」从**版本**段挪到**连接服务端**段——
它是**连接**的状态，用户想确认"连上的是哪台、什么版本"时目光应该在连接区，
不该翻到版本段去找。

---

## [0.1.16] — 2026-09-19
### 修：执行端那条下载路径**没有备用源**

执行端（局域网直连）通常是最快的下载源，所以 `latest.json` 里那两个公开源之外，
它也会被当成候选。但早先它只被设成"下载地址"、**没进候选列表**——
于是走这条路时**不测速、探不通也没有退路**，一条快路径变成了没有退路的路径。

现在它和公开源一样进候选，走同一套「测速 → 挑最快 → 都探不通就退下载页」的流程。

### 下载源怎么选：**自动测速，不列出来让你选**

点「下载新版本」时会**并发探测**所有候选源，挑最快的那个，然后告诉你用的是哪个：

```
下载源：Gitee（123ms）
```

为什么不让用户自己选：**他没法预判哪个快**——取决于他在哪、当时的网络，
而且会变。把选择推给他只能瞎猜，猜错了还得重下。

- 探测只取响应头、不读正文，超时很短——**别为了探测把下载拖住**
- 一个源都探不通 → 退回下载页，至少让你能自己点
- 执行端那个源要**核对版本**才用（它手里的文件可能是旧的）

---

## [0.1.15] — 2026-09-19
### 修：执行端的 `/update` 返回了**不合法的 JSON**

手拼 JSON 时漏了一个收尾引号：

```
{"version":"0.1.14",…,"apkVersion":"0.1.14        ← 少了 "}
```

服务端日志一片正常，**客户端拿到却直接解析失败**，表现成"检查更新坏了"。
已改用结构化拼装，并且**拼完自己先验一遍**——宁可当场炸，也别把坏 JSON 发出去。

顺带补了三个用例专门盯这个端点，其中一条就是"**必须能被 JSON 解析**"
（而不是拿字符串去 `contains`——那样漏引号照样通过）。

### 执行端会报出自己那个安装包的版本

配合上一条：执行端现在回 `apkVersion`（从**文件名**里读，`…-0.1.15.apk` → `0.1.15`）。
手机靠它决定"执行端这个下载源能不能用"——版本对不上就跳过，
不拿一个下完才发现不对的包给你。

解析写得很**严格**（整段必须恰好是 `X.Y.Z`）：读不出来就返回空串，
效果是手机跳过这个源——**这是安全的失败方向**。

---


### 修：产物名字里的 `+` 在 Gitee 上变成了空格

第一版命名用了 `android7+` / `macos11+`，结果 Gitee 上实际存成了：

```
aircontrol-daemon-macos11 -arm64-0.1.15.zip     ← 空格！
aircontrol-app-android7 -0.1.15.apk
```

**`+` 在表单编码里是空格的转义**，上传时被解码掉了。
于是 `latest.json` 里写的直链（带 `+`）指向一个不存在的文件——**404**。

改成 `min` 前缀，不含特殊字符：

```
aircontrol-app-android-min7-0.1.15.apk          ← Android 7 及以上
aircontrol-daemon-macos-min11-arm64-0.1.15.zip  ← macOS 11 及以上
```

### 修：`latest.json` 比实际发布**慢一拍**

它是在文档推送**之后**才生成的，于是赶不上那趟车——
公开仓的 commit 是 v0.1.15，里面的 `latest.json` 却还写着 0.1.14。
客户端因此永远看到上一个版本。

现在顺序固定为：**收产物 → 过安全闸 → 写 latest.json → 推文档 → 传产物**。

顺带发现**安全闸原来排在文档推送之后**——等于"已经发出去了再检查"，
那道闸就永远拦不住文档里的敏感数据。也一起挪到推之前了。

**实测端到端**：`latest.json` 里的直链 → HTTP 200 → 下到的包 `versionName='0.1.15'`。

---

## [0.1.14] — 2026-09-19
### 下载新版本：自动选**最快的源**

有多个下载源（Gitee / GitHub / 你自己的执行端），点「下载新版本」时会
**并发探测这几个源，挑最快的那个**，然后告诉你用的是哪个、多少毫秒。

为什么不让用户自己选：**他没法预判哪个快**——取决于他在哪、当时的网络，
而且会变。把选择推给他只能瞎猜；猜错了还得重下。这个判断机器做起来是几行代码，
人做起来是碰运气——**该机器做的别推给人**。

探测用的是「只取响应头、不读正文」的请求，超时给得很短：
探测可以慢一点，**但别为了探测把"下载"这件事拖住**。

一个源都探不通时退回下载页，至少让你能自己点。

### 执行端那个源要**核对版本**才用

执行端的 `/aircontrol.apk` 通常是最快的（局域网直连），但它手里的文件
**可能是旧的**——实测踩到：`/update` 报"最新 0.1.10"而文件其实是 0.1.9。

现在执行端会报出**自己那个文件的版本**，版本对不上就**跳过它**，
不拿一个下完才发现不对的包给你。

---

## [0.1.13] — 2026-09-19
### ⚠️ 修：macOS 包声明"要求 macOS 26"，在旧系统上根本装不了

实测发现执行端的原生库声明 `minos 26.0`——因为编译时**没指定最低系统版本**，
clang 就把当前 SDK 的版本写了进去。结果：**在 macOS 15 / 14 / 13 上都加载不了**，
而文件名里完全看不出来。

已修（显式 `-mmacosx-version-min=11.0`，现在声明 `minos 11.0`）。

### 产物命名写清「谁能装」

以前叫：

```
aircontrol-daemon-macos-arm64-0.1.12.zip
aircontrol-app-0.1.12.apk
```

**回答不了"最低哪个系统版本"**——用户下完在旧系统上跑不起来，只会以为包坏了。
现在从**产物自己身上读**最低版本，写进文件名：

```
aircontrol-daemon-macos11+-arm64-0.1.13.zip     ← macOS 11 及以上
aircontrol-app-android7+-0.1.13.apk             ← Android 7 及以上
```

读不出最低版本就**中止发布**——写不出这个的名字等于没写。

### 更新时**直接下载文件**，不是跳页面

以前 `latest.json` 只有一个版本号，所以"下载新版本"只能打开 release 页面，
让用户自己在里面挑包。现在 `latest.json` 里带**每个平台的直链**：

```json
{
  "client": "0.1.13",
  "releases": {
    "page":    "https://gitee.com/zhaoquan/aircontrol/releases",
    "android": { "minOs": "7", "url": "…/aircontrol-app-android7+-0.1.13.apk" },
    "macos":   { "minOs": "11", "arch": "arm64", "url": "…/aircontrol-daemon-macos11+-arm64-0.1.13.zip" }
  }
}
```

App 按**自己是什么平台**取对应的那条（现在只有 Android 一种形态，
将来出 iOS / 鸿蒙时按编译目标取，而不是"取第一个"——那会在那一刻静默下错包）。

「关于」里给的是**release 页面**（给人看的），更新下载用的是**直链**（给机器下的）——
两者不是一回事。

---

## [0.1.12] — 2026-09-19
### ⚠️ 修：发布的安装包**文件名和内容对不上**

实测发现线上几个版本的安装包是**整体错位一位**的：

| 文件名 | 里面实际是 |
|---|---|
| `aircontrol-app-0.1.11.apk` | 0.1.10 |
| `aircontrol-app-0.1.10.apk` | 0.1.9 |
| `aircontrol-app-0.1.9.apk` | 0.1.8 |

**症状就是你说的**：App 说"有 0.1.10"，下载装上却是 0.1.9。

原因是发布流程：改了版本号 → **中间没重新构建** → 发布脚本把旧包改个名字就传上去了。
发布脚本从来不检查包里到底是什么。

**现在加了闸**：打包前读产物**自己**的版本号，跟要发布的版本比对，
不一致就中止，并告诉你怎么重新构建。0.1.11 已重新构建并覆盖上传（两个平台都验过）。

### 设置页重新分块

原来是一长条没有分界的设置项，找东西只能靠滚。现在分三层：

- **连接服务端** —— 连接历史在最前（天天用的是它），「+ 新建连接」收起来
- **版本** —— 控制端 / 执行端版本 + 检查更新
- **关于**

### 连接串提升为**并列**的第三种方式

以前「粘贴连接串」挂在「IP 连接」下面，像是 IP 的附属品——
用户会以为必须先用 IP 才能用连接串。

其实它跟扫码、IP **完全平级**：扫码是"相机读"，IP 是"手输"，连接串是"粘贴"。
三种都是"我怎么把地址告诉它"，没有谁从属于谁。

现在三个并列：**[扫码连接] [IP 连接] [连接串]**

### 关于：写清楚数据怎么走 + 加下载地址

- 删掉了"源码不公开"——对用户没用，他关心的是数据去哪了
- 改成：**数据直接从你的执行端到你的手机，不经过我们的服务器。**
- 加了**安装包下载地址**（Gitee release 页）+ **一键复制**按钮
  （侧载没有应用商店，换机器或发给别人时得找得到包）

---

## [0.1.11] — 2026-09-19
### 首页重新排版：终端区域大了一截

连上之后天天用的是「终端 + 输入 + 虚拟键」，而打开大脑、截图、心跳、断开、
视图、布局这些都是**偶发**操作。它们原来常驻着，把终端挤掉好几行——
**手机上每一行像素都是终端行数**。

现在收进顶栏的「⋯ 控制」，默认收起，要用才展开。

### 虚拟键条改成两行（照 Termux 的排法）

```
第一行： ESC   ☰    ⌨   SHIFT   ↑   END   PGUP
第二行： TAB  CTRL  ALT    ←     ↓    →    ⏎
```

**方向键的排法是有讲究的**：↑ 和 ↓ 在**同一列**，← → 在下一行分别落在 ↓ 的左右。
这样四个键的相对位置跟真键盘一致，拇指不用重新学——排成一横排的话盲打必按错。

- **ESC** 给 vim / less
- **☰** 开抽屉
- **⌨** 拉起系统软键盘（虚拟键打不了普通字符，那些还得靠系统键盘）
- **END / PGUP** 翻页用

### 修饰键是粘滞的

**轻点 = 一次性**（下一个键组合执行后就自动取消），**长按 = 锁定**。
Ctrl 点一下变亮 → 再按 C 就是 Ctrl+C → 然后 Ctrl 自己灭掉。
不用按住不放（手机上也没法按住不放）。

### ⚠️ 没验证到的

- **修饰键高亮的视觉反馈**：逻辑和原生侧都在（`setModifier("tap"/"lock")`），
  但我模拟点击时没能确认那个高亮真的亮起来。**逻辑没验证到**，你上手试一下。
- 输入框跟随软键盘上推的精确位置：模拟器上不好复现真实键盘，需要真机确认。

---

## [0.1.10] — 2026-09-19
### 设备没连上时点会话，不再"点了没反应"

会话跑在远端那台机器上。设备没连上的时候点它，指令发出去只会石沉大海——
用户看到的就是**点了没反应**，只会以为坏了。

现在会说清楚，并给一个能直接修好的按钮：

```
打不开「work-1」
设备已断开连接
会话跑在远端那台机器上，得先连上才能打开。
                    [重连]  [取消]
```

### 「重连」是**先断开、再连**

不是直接叠一条新连接。旧连接可能还半死不活地占着
（TCP 没断干净、服务端还认着它），直接再连会变成两条连接抢一个设备、
状态互相打架。先干净地摘掉再挂上——交互上也就不会有"点了没反应"的空窗。

### 抽屉里也能看出连接状态

设备那一行现在跟主页面的「连接历史」用**同一套图标**：

| 状态 | 图标 |
|---|---|
| 已连接 | 🟢 |
| 连接中 | ◌ |
| 连接失败 / 已断开 | ❗ |

同一个概念在两处长得不一样的话，用户得学两遍。

### 修：抽屉底部被盖住

抽屉、设置、连接弹窗原来是画在命令输入行**之前**的，
于是最下面那条输入行和按键行**盖在抽屉上面**——
抽屉底部放的「⚙ 设置」根本看不见。

现在浮层统一在主内容**之后**渲染（最后画 = 在最上层）。

---

## [0.1.9] — 2026-09-19
### ⚠️ 修了一个「一台手机连着，另一台就永远连不上」的 bug

这个 bug 很难查，因为**症状是"一直连接中"**，看起来像网络问题：

服务端**只允许一个客户端连接**。第二台手机连上来时，
TCP 和 WebSocket 握手**都成功**（所以看起来"连上了"），
然后服务端**立刻把它关掉**——App 于是不断重试，用户看到的就是永远转圈。

所以：**只要有一台设备连着，别的设备就连不上**（包括你自己换个 IP 再连）。
现在支持多台同时在线。

> 这也跟产品自己的功能矛盾：数据模型支持多设备，App 上还有「+ 新建连接」，
> 而服务端只认一台。

### 首页改成「连接历史」，每条带状态

原来那一块叫「已连接」，只列当前通着的那台。现在列**你连过的每一台**，
并显示它现在的状态：

| 状态 | 图标 |
|---|---|
| 已连接 | 🟢 绿色小点 |
| 连接中 | 转圈 |
| 连接失败 | ✕ 红色 |
| 已断开连接 | ❗ 黄色 |

断了的、失败的同样要看得到——否则你不知道刚才那次到底怎么了。

### 修：状态显示不刷新

上面那个状态一开始是**写死的**：明明已经连上了，还一直显示「已断开连接」。

原因是状态在 Kuikly 的**响应式作用域之外**求值了一次。
Kuikly 只追踪 `attr{}` / `vif{}` **里面**的属性读取，
外面算的值会永远停在构建那一刻。已改成在作用域内现算。

### 布局：浮层要最后画

抽屉、设置、连接弹窗原来画在命令输入行**之前**，
于是底部的输入行和按键行盖在它们上面。
现在浮层统一在主内容之后渲染（最后画 = 在最上层）。

### 界面调整

- 设置齿轮移到**抽屉底部**（设置是低频操作，顶栏那点位置留给高频的会话切换）
- 顶栏的「⚙ 设置」只在**一台设备都没连**时出现——否则新用户不知道从哪开始

---

## [0.1.8] — 2026-09-19
### 修：点「检查更新」总是说"查得太频繁"

**明明查得到，却告诉你查不了。** 两个原因叠在一起：

1. **某个源限流就把整件事判死** —— Gitee 的接口返 403（限流），
   而 GitHub 明明是通的，代码却在 Gitee 那一步直接放弃了。
   现在改成：一个源不行就**接着试下一个**，全部都被限流才说"频繁"。

2. **Gitee 的接口对未认证请求直接限流** ——
   `403 Forbidden (Rate Limit Exceeded)`，带令牌才放行。
   而 App 不可能带令牌（那等于把令牌发给所有人）。

   所以改成读仓库里的一个**普通文件**，不走接口：
   `https://gitee.com/zhaoquan/aircontrol/raw/main/latest.json`

   走文件读取没有限流问题。这个文件每次发版由发布脚本自动更新。

### 顺带

现在「检查更新」的来源顺序是：
**执行端** → **Gitee 文件** → **GitHub 文件** → Gitee 接口 → GitHub 接口。
前三个都是不限流的，后两个只是兜底。

---

## [0.1.7] — 2026-09-19
这一版把**连接**这件事整个重做了。

### 点「连接」之后有反应了

之前点下去看不出任何动静。现在会立刻弹一个窗口：

```
        ⏳ 连接中
      192.168.1.100:8080
         [取消]
```

- **超时会明说超时**（12 秒），弹窗变成「连接失败」+ 重试/取消
- 连上就自动收掉

你要的从来不是"还在试"，是"到底行不行"。

### 连上之后不再摆一堆输入框

主页面变成这样：

```
已连接
  ● 已连接：192.168.1.100:8080:1234   [重新连接] [断开]
  [+ 新建连接]
```

地址、端口、PIN 是**一次性配置**，长期占着首屏没意义。
想再连一台就点「新建连接」，它就支持多设备。

> 这一行的 PIN 是**明文**的——它是给你核对"我连的到底是哪台"用的。
> 历史列表里仍然打码，因为那是个列表，容易被旁边的人扫到。

### 历史连接变成一个有状态的列表

每条记录现在能看出**它现在是什么状态**，并且直接能操作：

| 显示 | 动作 |
|---|---|
| ● `地址` `••••` + **已连接** | **重新连接** / 修改 / 删除 |
| ○ `地址` `••••` + 未连接 | **连接** / 修改 / 删除 |

**端口或 PIN 变了**就点「修改」——值会填回表单，改完连上会**更新原来那条**，
而不是新增一条（否则列表越改越乱）。

### 修了一个 bug

**扫码或粘贴连接串连上的机器，从来不会出现在历史里**——
只有手动填表单那条路会记录。已修。

---

## [0.1.6] — 2026-09-19
### 装之前和装之后，版本号终于对得上了

之前**安装界面显示的版本号**和**装完打开看到的**是两回事：

| | 显示 |
|---|---|
| Android 安装界面 | `0.0.1`（写死的，从没改过） |
| 装完打开，设置页 | `0.1.x`（真实版本） |

安装过程里冒出一个对不上的版本号，很容易让人怀疑"是不是装错了包"。

现在**只有一处版本号**，安装界面和 App 内部都从它来。顺带把系统的
`versionCode` 也从写死的 `1` 改成随版本递增——之前它永远是 1，
等于**从来不告诉系统"这是个新版本"**。

---

## [0.1.5] — 2026-09-19
两个都是**用起来才会发现**的问题。

### 隐私：地址示例里不该出现真实 IP

之前地址框的示例写的是一个**真实的 Tailscale 地址**——那是开发机上在用的地址，
编进包里发给所有人。别人照着填，**有可能连到开发者的机器上**。

现在示例是通用的 `192.168.1.100`。

> 说明：连过一次之后，地址框会用**你自己上次连过的那台**预填。
> 那份记录只存在**你的手机上**，不会发给任何人。

### 按钮有按下反馈了

之前点按钮**看不出点没点上**。现在按下去会明显变暗一档——
尤其「连接」这种点完还要等网络的，没反馈时最容易被当成没反应而反复点。

顺带：点「连接」会**立刻**在状态栏写一句「连接中 x.x.x.x:8080 …」，
不用干等着猜。

---

## [0.1.4] — 2026-09-19
这一版改的是**会话**——之前那套模型是错的。

### 你机器上的会话，现在都看得见了

以前 App 里的「会话」其实是**某一个 tmux 会话里的窗口**，而且只认启动时指定的那一个。
结果：你自己 `tmux new -s work-1` 建的会话，**在 App 里根本不存在**。
（实测：机器上有 4 个 tmux 会话，App 只看得到 1 个。）

现在「会话」就是**你机器上的 tmux 会话**——包括不是我们建的。打开抽屉能看到全部。

### 抽屉里能新建会话了

会话分组下面多了「**+ 新建会话**」。名字我们替你起好（`session-1`、`session-2`…），
不用先想名字。建好之后可以直接在里面起 AI。

### 关掉过的会话，能重新打开

重启机器之后 tmux 会话会没。以前它们就**凭空消失**了；现在会**留在列表里**，
显示成「已结束 · 之前跑 xxx」，右边一个「**重新打开**」。

点它会：

1. 起在**原来那个目录**（不在原目录，CLI 读不到工程，等于开个空壳）
2. 起**原来那个 CLI**
3. 告诉它「我要接着上次」——**然后由 CLI 弹它自己的选择器，你自己挑哪一次**

> Claude Code 是敲 `/resume`，Codex 是起 `resume` 子命令。两者都会列出历史会话让你选。
>
> **我们不记你的会话 id**，也不替你选。那需要读 CLI 自己的存储格式，**那是它的东西**。
> 记的只是「跑什么命令、在哪个目录」——那本来就是启动它时我们知道的东西。

不想再看见某条记录？**长按**它即可。

### 修好的问题

- 连接设置里的默认地址不再预填 `<模拟器宿主别名>`（那是**模拟器专用**地址，真机上根本不存在，
  会让人一直卡在「连接中」还不知道为什么）。现在改成用**你上次连过的那台**预填。

### ⚠️ 这个版本要注意的

- **扫码仍未经真机验证**：只验到相机能打开，真正扫出内容没验过。扫不出来请用 IP 连接。
- 屏幕上仍然只能看、不能点

---

## [0.1.3] — 2026-09-19
这一版解决的是「**怎么装**」和「**怎么连**」。

### 让手机直接扫一下就配对

执行端启动时会在终端里打印一个二维码。App 的「设置 → 连接方式」里多了
**扫码连接**：对着那个二维码扫一下，**地址和 PIN 自动填好并直接连上**。

不用再手敲 IP，也不用把连接串复制来复制去。

> 连接方式现在是**并列的两种**：扫码连接 / IP 连接。想手填就选 IP 连接，
> 每个输入框上面都写清了「填什么」和「从哪看」。
> 拿到连接串的话，也可以直接粘进「粘贴连接串」。

### 一条命令装执行端

```bash
curl -fsSL https://gitee.com/zhaoquan/aircontrol/raw/main/install.sh | bash
```

它会：挑对版本 → **从能通的源下载**（国内自动走 Gitee）→ **校验 SHA-256** → 装好并打印配对信息。

加 `--start` 装完直接启动；`--uninstall` 卸载（**不会动 tmux 里正在跑的会话**）。
重复执行就是升级。

### 从通知、聊天里直接点进应用

`aircontrol://` 链接现在能直接打开 App：

- 别人发给你一条连接串 → 点一下 → 自动连上那台机器
- 点「大脑在等你批准」的通知 → 直接到审批

### ⚠️ 这个版本要注意的

- **扫码需要真机验证**：目前只在模拟器上验过相机能打开，
  **真正扫出二维码内容还没在真机上验过**。扫不出来请先用「IP 连接」。
- 屏幕上仍然只能看、不能点
- Windows 上 `install.sh` 跑不了（它是 bash 脚本），需要手动下载解压

---

## [0.1.2] — 2026-09-19
这一版主要在**界面**和**"怎么拿到新版本"**上。

### 你能做的事

**左侧会话抽屉：一眼看出哪台机器、哪个会话在等你**

顶上多了「☰ 会话」。点开是一个抽屉，按**设备**和**会话**分组：

```
设备
  ● <模拟器宿主别名>                    已连接
会话 · <模拟器宿主别名>
  ○ zsh                          zsh
  ● claude                    2.1.278
```

每条前面有个**状态点**，颜色就是它的意思：

| 颜色 | 意思 |
|---|---|
| 🟡 **黄** | **有人在等你**——AI 要你批准某个操作 |
| 🔵 蓝 | 正在跑 |
| ⚪ 灰 | 空闲 |
| 🔴 红 | 出错了 |

**黄色排在最优先**：正在跑不稀奇，卡住等你才要紧。顶栏也放了一个当前状态的点，
抽屉关着时也能瞥一眼。

点一条就切过去。**切换不会打断其它会话**——它们在后台照常跑着。

> 只有**你当前看的那个会话**的状态是准的；别的会话显示灰点，
> 因为我们确实不知道它们在干什么——**不知道就不编**。

**「设置」独立成一页**

主界面回归成纯粹的控制台。连地址、PIN、历史连接、版本信息都收进了「设置」。
主界面没连上时只有一句话和一个「打开设置」按钮。

**检查更新（只提示，不代装）**

设置页最下面有「检查更新」。有新版本会在那里显示，并给一个「下载新版本」按钮——
**点不点、装不装，你自己决定**。App 不会偷偷下载，也不会自己把自己换掉。

**国内下载走 Gitee**

GitHub 的**下载**会跳到一个国内常常连不上的域名——页面能打开、一点下载就没进度。
所以国内用户请用镜像仓：

| | 地址 |
|---|---|
| **Gitee（国内，推荐）** | https://gitee.com/zhaoquan/aircontrol/releases |
| GitHub | https://github.com/chatterzhao/aircontrol/releases |

两个仓库内容一样，同版本同时发布。**App 检查更新时也会优先走能通的源。**

### 修好的问题

- 会话列表里点「上下」布局不生效（面板还是左右排的）
- 更新检查把「问得太频繁」误报成「连不上网」——现在会分开说

### ⚠️ 这个版本要注意的

- **屏幕仍然只能看，不能点**
- **执行端不做自动更新**：它有系统权限，自动替换自己的风险太大。
  更新方式是**下载新版本重新启动**，会话不会丢（它们在 tmux 里）
- Windows / Linux 的屏幕捕获仍走降级方案，比 macOS 慢

---

## [0.1.1] — 2026-09-19
> 这一版发得比较急，改动也小，主要是给 0.1.2 打底。补记在这里，免得版本号断档。

- App 设置页里能看到**执行端版本和协议版本**了
- 连不上或版本对不上时，会给出**能看懂的原因**，而不是只显示一个失败

---

## [0.1.0] — 2026-09-19
**首个开发预览版。** 现在可以用手机指挥电脑上的 AI CLI 了。

### 你能做的事

**真终端，不是截图**

手机上是**真正的终端**，不是把电脑屏幕截个图给你看。
`vim`、`htop`、`claude` 这类全屏程序能正常操作，滚动、光标、颜色都对。
终端大小按**你手机屏幕**算，所以排版是给手机看的，不是把电脑的宽屏压扁。

**看屏幕**

有些东西终端里看不见——微信消息、聊天工具、系统安装器的弹窗。
切到「屏幕」视图就能看到电脑现在长什么样。**目前只能看，还不能点。**

**同时开多个 AI**

点「打开」选一个 AI CLI，会**新开一个会话**跑它，原来那个不受影响。
所以可以一边让 Claude Code 改代码、一边开个 Codex 问别的。
上面有会话条可以来回切。

**AI 要你批准时会通知你**

AI 准备执行危险命令（比如删文件、跑脚本）时会停下来等你。
**通知栏直接推给你**，点开就能看完整命令再决定批不批。
你不在手机前、或者断线了，它会交还给 AI 自己处理——**我们不替它做决定**。

**断线了不会丢**

锁屏、切 Wi-Fi、切后台再回来，会话还在，终端内容还在。
因为会话跑在电脑上（tmux 里），手机只是接上去看。

**连过的机器会记住**

连过一次的地址、端口、PIN 会存在手机里，下次点一下就回填。
PIN 在列表里是打码显示的。可以删单条，也可以一键清空。

### 支持的平台

| 部分 | 平台 |
|---|---|
| 执行端 | macOS、Windows 10 及以上、Linux (x64) |
| 控制端 | Android |

执行端需要 **Java 17 或更高版本**。

### ⚠️ 这个版本要注意的

- **链路是明文 `ws://`**：请**只在 Tailscale 或可信局域网内使用**。
  用 Tailscale 时整条链路有 WireGuard 加密；纯公网直连**不建议**。
- **屏幕只能看，不能点**：点击操作还没做。
- **屏幕画面帧率不高**（约 5 帧/秒）：看静态内容（比如弹窗）够用，不是看视频。
- **Windows / Linux 的屏幕捕获**走的是降级方案，比 macOS 慢。
- 界面还在打磨，你可能会觉得有些地方不顺手——欢迎提 Issue。
